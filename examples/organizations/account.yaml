apiVersion: organizations.aws.upbound.io/v1beta1
kind: Account
metadata:
  annotations:
    meta.upbound.io/example-id: organizations/v1beta1/account
    upjet.upbound.io/manual-intervention: "This resource requires an organization, manually tested with a free tier account"
  labels:
    testing.upbound.io/example-name: account
  name: account
spec:
  forProvider:
    email: <new-account-email>
    name: my_new_account
    region: us-west-1
