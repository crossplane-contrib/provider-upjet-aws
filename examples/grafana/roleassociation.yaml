apiVersion: grafana.aws.upbound.io/v1beta1
kind: RoleAssociation
metadata:
  name: example
spec:
  forProvider:
    region: us-east-1
    role: ADMIN
    userIds:
    - USER_ID_1
    workspaceIdRef:
      name: example
