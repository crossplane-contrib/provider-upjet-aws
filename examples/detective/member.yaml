apiVersion: detective.aws.upbound.io/v1beta1
kind: Member
metadata:
  annotations:
    upjet.upbound.io/manual-intervention: "Requires AccountId and AccountEmail to replaced manually."
  labels:
    testing.upbound.io/example-name: example
  name: example
spec:
  forProvider:
    accountId: <AccountId>
    disableEmailNotification: true
    emailAddress: <AccountEmail>
    graphArnSelector:
      matchLabels:
        testing.upbound.io/example-name: example
    message: Message of the invitation
    region: us-west-1
---
apiVersion: detective.aws.upbound.io/v1beta1
kind: Graph
metadata:
  annotations:
    upjet.upbound.io/manual-intervention: "Requires AccountId and AccountEmail to replaced manually."
  labels:
    testing.upbound.io/example-name: example
  name: example
spec:
  forProvider:
    region: us-west-1