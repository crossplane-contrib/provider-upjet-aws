apiVersion: lexmodels.aws.upbound.io/v1beta1
kind: BotAlias
metadata:
  name: example
spec:
  forProvider:
    region: us-east-1
    botName: example
    botVersion: $LATEST
    description: Production Version of the OrderFlowers Bot.
