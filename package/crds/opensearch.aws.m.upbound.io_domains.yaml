---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: domains.opensearch.aws.m.upbound.io
spec:
  group: opensearch.aws.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Domain is the Schema for the Domains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of Domain
            properties:
              forProvider:
                properties:
                  advancedOptions:
                    additionalProperties:
                      type: string
                    description: Key-value string pairs to specify advanced configuration
                      options.
                    type: object
                    x-kubernetes-map-type: granular
                  advancedSecurityOptions:
                    description: Configuration block for fine-grained access control.
                      Detailed below.
                    properties:
                      anonymousAuthEnabled:
                        description: Whether Anonymous auth is enabled. Enables fine-grained
                          access control on an existing domain. Ignored unless advanced_security_options
                          are enabled. Can only be enabled on an existing domain.
                        type: boolean
                      enabled:
                        description: Whether advanced security is enabled.
                        type: boolean
                      internalUserDatabaseEnabled:
                        description: Whether the internal user database is enabled.
                          Default is false.
                        type: boolean
                      masterUserOptions:
                        description: Configuration block for the main user. Detailed
                          below.
                        properties:
                          masterUserArn:
                            description: ARN for the main user. Only specify if internal_user_database_enabled
                              is not set or set to false.
                            type: string
                          masterUserName:
                            description: Main user's username, which is stored in
                              the Amazon OpenSearch Service domain's internal database.
                              Only specify if internal_user_database_enabled is set
                              to true.
                            type: string
                          masterUserPasswordSecretRef:
                            description: Main user's password, which is stored in
                              the Amazon OpenSearch Service domain's internal database.
                              Only specify if internal_user_database_enabled is set
                              to true.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                    type: object
                  autoTuneOptions:
                    description: Configuration block for the Auto-Tune options of
                      the domain. Detailed below.
                    properties:
                      desiredState:
                        description: 'Auto-Tune desired state for the domain. Valid
                          values: ENABLED or DISABLED.'
                        type: string
                      maintenanceSchedule:
                        description: Configuration block for Auto-Tune maintenance
                          windows. Can be specified multiple times for each maintenance
                          window. Detailed below.
                        items:
                          properties:
                            cronExpressionForRecurrence:
                              description: A cron expression specifying the recurrence
                                pattern for an Auto-Tune maintenance schedule.
                              type: string
                            duration:
                              description: Configuration block for the duration of
                                the Auto-Tune maintenance window. Detailed below.
                              properties:
                                unit:
                                  description: 'Unit of time specifying the duration
                                    of an Auto-Tune maintenance window. Valid values:
                                    HOURS.'
                                  type: string
                                value:
                                  description: An integer specifying the value of
                                    the duration of an Auto-Tune maintenance window.
                                  type: number
                              type: object
                            startAt:
                              description: Date and time at which to start the Auto-Tune
                                maintenance schedule in RFC3339 format.
                              type: string
                          type: object
                        type: array
                      rollbackOnDisable:
                        description: 'Whether to roll back to default Auto-Tune settings
                          when disabling Auto-Tune. Valid values: DEFAULT_ROLLBACK
                          or NO_ROLLBACK.'
                        type: string
                      useOffPeakWindow:
                        description: Whether to schedule Auto-Tune optimizations that
                          require blue/green deployments during the domain's configured
                          daily off-peak window. Defaults to false.
                        type: boolean
                    type: object
                  clusterConfig:
                    description: Configuration block for the cluster of the domain.
                      Detailed below.
                    properties:
                      coldStorageOptions:
                        description: Configuration block containing cold storage configuration.
                          Detailed below.
                        properties:
                          enabled:
                            description: Boolean to enable cold storage for an OpenSearch
                              domain. Defaults to false. Master and ultrawarm nodes
                              must be enabled for cold storage.
                            type: boolean
                        type: object
                      dedicatedMasterCount:
                        description: Number of dedicated main nodes in the cluster.
                        type: number
                      dedicatedMasterEnabled:
                        description: Whether dedicated main nodes are enabled for
                          the cluster.
                        type: boolean
                      dedicatedMasterType:
                        description: Instance type of the dedicated main nodes in
                          the cluster.
                        type: string
                      instanceCount:
                        description: Number of instances in the cluster.
                        type: number
                      instanceType:
                        description: Instance type of data nodes in the cluster.
                        type: string
                      multiAzWithStandbyEnabled:
                        description: Whether a multi-AZ domain is turned on with a
                          standby AZ. For more information, see Configuring a multi-AZ
                          domain in Amazon OpenSearch Service.
                        type: boolean
                      nodeOptions:
                        description: List of node options for the domain.
                        items:
                          properties:
                            nodeConfig:
                              description: Container to specify sizing of a node type.
                              properties:
                                count:
                                  description: Number of nodes of a particular node
                                    type in the cluster.
                                  type: number
                                enabled:
                                  description: Whether a particular node type is enabled.
                                  type: boolean
                                type:
                                  description: The instance type of a particular node
                                    type in the cluster.
                                  type: string
                              type: object
                            nodeType:
                              description: 'Type of node this configuration describes.
                                Valid values: coordinator.'
                              type: string
                          type: object
                        type: array
                      warmCount:
                        description: Number of warm nodes in the cluster. Valid values
                          are between 2 and 150. warm_count can be only and must be
                          set when warm_enabled is set to true.
                        type: number
                      warmEnabled:
                        description: Whether to enable warm storage.
                        type: boolean
                      warmType:
                        description: Instance type for the OpenSearch cluster's warm
                          nodes. Valid values are ultrawarm1.medium.search, ultrawarm1.large.search
                          and ultrawarm1.xlarge.search. warm_type can be only and
                          must be set when warm_enabled is set to true.
                        type: string
                      zoneAwarenessConfig:
                        description: Configuration block containing zone awareness
                          settings. Detailed below.
                        properties:
                          availabilityZoneCount:
                            description: 'Number of Availability Zones for the domain
                              to use with zone_awareness_enabled. Defaults to 2. Valid
                              values: 2 or 3.'
                            type: number
                        type: object
                      zoneAwarenessEnabled:
                        description: Whether zone awareness is enabled, set to true
                          for multi-az deployment. To enable awareness with three
                          Availability Zones, the availability_zone_count within the
                          zone_awareness_config must be set to 3.
                        type: boolean
                    type: object
                  cognitoOptions:
                    description: Configuration block for authenticating dashboard
                      with Cognito. Detailed below.
                    properties:
                      enabled:
                        description: Whether Amazon Cognito authentication with Dashboard
                          is enabled or not. Default is false.
                        type: boolean
                      identityPoolId:
                        description: ID of the Cognito Identity Pool to use.
                        type: string
                      roleArn:
                        description: ARN of the IAM role that has the AmazonOpenSearchServiceCognitoAccess
                          policy attached.
                        type: string
                      userPoolId:
                        description: ID of the Cognito User Pool to use.
                        type: string
                    type: object
                  domainEndpointOptions:
                    description: Configuration block for domain endpoint HTTP(S) related
                      options. Detailed below.
                    properties:
                      customEndpoint:
                        description: Fully qualified domain for your custom endpoint.
                        type: string
                      customEndpointCertificateArn:
                        description: ACM certificate ARN for your custom endpoint.
                        type: string
                      customEndpointEnabled:
                        description: Whether to enable custom endpoint for the OpenSearch
                          domain.
                        type: boolean
                      enforceHttps:
                        description: Whether or not to require HTTPS. Defaults to
                          true.
                        type: boolean
                      tlsSecurityPolicy:
                        description: Name of the TLS security policy that needs to
                          be applied to the HTTPS endpoint. For valid values, refer
                          to the AWS documentation.
                        type: string
                    type: object
                  domainName:
                    description: Name of the domain.
                    type: string
                  ebsOptions:
                    description: Configuration block for EBS related options, may
                      be required based on chosen instance size. Detailed below.
                    properties:
                      ebsEnabled:
                        description: Whether EBS volumes are attached to data nodes
                          in the domain.
                        type: boolean
                      iops:
                        description: Baseline input/output (I/O) performance of EBS
                          volumes attached to data nodes. Applicable only for the
                          GP3 and Provisioned IOPS EBS volume types.
                        type: number
                      throughput:
                        description: Specifies the throughput (in MiB/s) of the EBS
                          volumes attached to data nodes. Applicable only for the
                          gp3 volume type.
                        type: number
                      volumeSize:
                        description: Size of EBS volumes attached to data nodes (in
                          GiB).
                        type: number
                      volumeType:
                        description: Type of EBS volumes attached to data nodes.
                        type: string
                    type: object
                  encryptAtRest:
                    description: Configuration block for encrypt at rest options.
                      Only available for certain instance types. Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable encryption at rest. If the
                          encrypt_at_rest block is not provided then this defaults
                          to false. Enabling encryption on new domains requires an
                          engine_version of OpenSearch_X.Y or Elasticsearch_5.1 or
                          greater.
                        type: boolean
                      kmsKeyId:
                        description: KMS key ARN to encrypt the Elasticsearch domain
                          with. If not specified then it defaults to using the aws/es
                          service KMS key. Note that KMS will accept a KMS key ID
                          but will return the key ARN.
                        type: string
                      kmsKeyIdRef:
                        description: Reference to a Key in kms to populate kmsKeyId.
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          namespace:
                            description: Namespace of the referenced object
                            type: string
                          policy:
                            description: Policies for referencing.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        required:
                        - name
                        type: object
                      kmsKeyIdSelector:
                        description: Selector for a Key in kms to populate kmsKeyId.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                    type: object
                  engineVersion:
                    description: while Elasticsearch has elasticsearch_version
                    type: string
                  ipAddressType:
                    description: The IP address type for the endpoint. Valid values
                      are ipv4 and dualstack.
                    type: string
                  logPublishingOptions:
                    description: Configuration block for publishing slow and application
                      logs to CloudWatch Logs. This block can be declared multiple
                      times, for each log_type, within the same resource. Detailed
                      below.
                    items:
                      properties:
                        cloudwatchLogGroupArn:
                          description: ARN of the Cloudwatch log group to which log
                            needs to be published.
                          type: string
                        cloudwatchLogGroupArnRef:
                          description: Reference to a Group in cloudwatchlogs to populate
                            cloudwatchLogGroupArn.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        cloudwatchLogGroupArnSelector:
                          description: Selector for a Group in cloudwatchlogs to populate
                            cloudwatchLogGroupArn.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        enabled:
                          description: Whether given log publishing option is enabled
                            or not.
                          type: boolean
                        logType:
                          description: 'Type of OpenSearch log. Valid values: INDEX_SLOW_LOGS,
                            SEARCH_SLOW_LOGS, ES_APPLICATION_LOGS, AUDIT_LOGS.'
                          type: string
                      type: object
                    type: array
                  nodeToNodeEncryption:
                    description: Configuration block for node-to-node encryption options.
                      Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable node-to-node encryption. If
                          the node_to_node_encryption block is not provided then this
                          defaults to false. Enabling node-to-node encryption of a
                          new domain requires an engine_version of OpenSearch_X.Y
                          or Elasticsearch_6.0 or greater.
                        type: boolean
                    type: object
                  offPeakWindowOptions:
                    description: Configuration to add Off Peak update options. (documentation).
                      Detailed below.
                    properties:
                      enabled:
                        description: Enabled disabled toggle for off-peak update window.
                        type: boolean
                      offPeakWindow:
                        properties:
                          windowStartTime:
                            description: 10h window for updates
                            properties:
                              hours:
                                description: Starting hour of the 10-hour window for
                                  updates
                                type: number
                              minutes:
                                description: Starting minute of the 10-hour window
                                  for updates
                                type: number
                            type: object
                        type: object
                    type: object
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  snapshotOptions:
                    description: Configuration block for snapshot related options.
                      Detailed below. DEPRECATED. For domains running OpenSearch 5.3
                      and later, Amazon OpenSearch takes hourly automated snapshots,
                      making this setting irrelevant. For domains running earlier
                      versions, OpenSearch takes daily automated snapshots.
                    properties:
                      automatedSnapshotStartHour:
                        description: Hour during which the service takes an automated
                          daily snapshot of the indices in the domain.
                        type: number
                    type: object
                  softwareUpdateOptions:
                    description: Software update options for the domain. Detailed
                      below.
                    properties:
                      autoSoftwareUpdateEnabled:
                        description: Whether automatic service software updates are
                          enabled for the domain. Defaults to false.
                        type: boolean
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcOptions:
                    description: Configuration block for VPC related options. Adding
                      or removing this configuration forces a new resource (documentation).
                      Detailed below.
                    properties:
                      securityGroupIdRefs:
                        description: References to SecurityGroup in ec2 to populate
                          securityGroupIds.
                        items:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      securityGroupIdSelector:
                        description: Selector for a list of SecurityGroup in ec2 to
                          populate securityGroupIds.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      securityGroupIds:
                        description: List of VPC Security Group IDs to be applied
                          to the OpenSearch domain endpoints. If omitted, the default
                          Security Group for the VPC will be used.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      subnetIdRefs:
                        description: References to Subnet in ec2 to populate subnetIds.
                        items:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      subnetIdSelector:
                        description: Selector for a list of Subnet in ec2 to populate
                          subnetIds.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      subnetIds:
                        description: List of VPC Subnet IDs for the OpenSearch domain
                          endpoints to be created in.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                required:
                - region
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  advancedOptions:
                    additionalProperties:
                      type: string
                    description: Key-value string pairs to specify advanced configuration
                      options.
                    type: object
                    x-kubernetes-map-type: granular
                  advancedSecurityOptions:
                    description: Configuration block for fine-grained access control.
                      Detailed below.
                    properties:
                      anonymousAuthEnabled:
                        description: Whether Anonymous auth is enabled. Enables fine-grained
                          access control on an existing domain. Ignored unless advanced_security_options
                          are enabled. Can only be enabled on an existing domain.
                        type: boolean
                      enabled:
                        description: Whether advanced security is enabled.
                        type: boolean
                      internalUserDatabaseEnabled:
                        description: Whether the internal user database is enabled.
                          Default is false.
                        type: boolean
                      masterUserOptions:
                        description: Configuration block for the main user. Detailed
                          below.
                        properties:
                          masterUserArn:
                            description: ARN for the main user. Only specify if internal_user_database_enabled
                              is not set or set to false.
                            type: string
                          masterUserName:
                            description: Main user's username, which is stored in
                              the Amazon OpenSearch Service domain's internal database.
                              Only specify if internal_user_database_enabled is set
                              to true.
                            type: string
                          masterUserPasswordSecretRef:
                            description: Main user's password, which is stored in
                              the Amazon OpenSearch Service domain's internal database.
                              Only specify if internal_user_database_enabled is set
                              to true.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        type: object
                    type: object
                  autoTuneOptions:
                    description: Configuration block for the Auto-Tune options of
                      the domain. Detailed below.
                    properties:
                      desiredState:
                        description: 'Auto-Tune desired state for the domain. Valid
                          values: ENABLED or DISABLED.'
                        type: string
                      maintenanceSchedule:
                        description: Configuration block for Auto-Tune maintenance
                          windows. Can be specified multiple times for each maintenance
                          window. Detailed below.
                        items:
                          properties:
                            cronExpressionForRecurrence:
                              description: A cron expression specifying the recurrence
                                pattern for an Auto-Tune maintenance schedule.
                              type: string
                            duration:
                              description: Configuration block for the duration of
                                the Auto-Tune maintenance window. Detailed below.
                              properties:
                                unit:
                                  description: 'Unit of time specifying the duration
                                    of an Auto-Tune maintenance window. Valid values:
                                    HOURS.'
                                  type: string
                                value:
                                  description: An integer specifying the value of
                                    the duration of an Auto-Tune maintenance window.
                                  type: number
                              type: object
                            startAt:
                              description: Date and time at which to start the Auto-Tune
                                maintenance schedule in RFC3339 format.
                              type: string
                          type: object
                        type: array
                      rollbackOnDisable:
                        description: 'Whether to roll back to default Auto-Tune settings
                          when disabling Auto-Tune. Valid values: DEFAULT_ROLLBACK
                          or NO_ROLLBACK.'
                        type: string
                      useOffPeakWindow:
                        description: Whether to schedule Auto-Tune optimizations that
                          require blue/green deployments during the domain's configured
                          daily off-peak window. Defaults to false.
                        type: boolean
                    type: object
                  clusterConfig:
                    description: Configuration block for the cluster of the domain.
                      Detailed below.
                    properties:
                      coldStorageOptions:
                        description: Configuration block containing cold storage configuration.
                          Detailed below.
                        properties:
                          enabled:
                            description: Boolean to enable cold storage for an OpenSearch
                              domain. Defaults to false. Master and ultrawarm nodes
                              must be enabled for cold storage.
                            type: boolean
                        type: object
                      dedicatedMasterCount:
                        description: Number of dedicated main nodes in the cluster.
                        type: number
                      dedicatedMasterEnabled:
                        description: Whether dedicated main nodes are enabled for
                          the cluster.
                        type: boolean
                      dedicatedMasterType:
                        description: Instance type of the dedicated main nodes in
                          the cluster.
                        type: string
                      instanceCount:
                        description: Number of instances in the cluster.
                        type: number
                      instanceType:
                        description: Instance type of data nodes in the cluster.
                        type: string
                      multiAzWithStandbyEnabled:
                        description: Whether a multi-AZ domain is turned on with a
                          standby AZ. For more information, see Configuring a multi-AZ
                          domain in Amazon OpenSearch Service.
                        type: boolean
                      nodeOptions:
                        description: List of node options for the domain.
                        items:
                          properties:
                            nodeConfig:
                              description: Container to specify sizing of a node type.
                              properties:
                                count:
                                  description: Number of nodes of a particular node
                                    type in the cluster.
                                  type: number
                                enabled:
                                  description: Whether a particular node type is enabled.
                                  type: boolean
                                type:
                                  description: The instance type of a particular node
                                    type in the cluster.
                                  type: string
                              type: object
                            nodeType:
                              description: 'Type of node this configuration describes.
                                Valid values: coordinator.'
                              type: string
                          type: object
                        type: array
                      warmCount:
                        description: Number of warm nodes in the cluster. Valid values
                          are between 2 and 150. warm_count can be only and must be
                          set when warm_enabled is set to true.
                        type: number
                      warmEnabled:
                        description: Whether to enable warm storage.
                        type: boolean
                      warmType:
                        description: Instance type for the OpenSearch cluster's warm
                          nodes. Valid values are ultrawarm1.medium.search, ultrawarm1.large.search
                          and ultrawarm1.xlarge.search. warm_type can be only and
                          must be set when warm_enabled is set to true.
                        type: string
                      zoneAwarenessConfig:
                        description: Configuration block containing zone awareness
                          settings. Detailed below.
                        properties:
                          availabilityZoneCount:
                            description: 'Number of Availability Zones for the domain
                              to use with zone_awareness_enabled. Defaults to 2. Valid
                              values: 2 or 3.'
                            type: number
                        type: object
                      zoneAwarenessEnabled:
                        description: Whether zone awareness is enabled, set to true
                          for multi-az deployment. To enable awareness with three
                          Availability Zones, the availability_zone_count within the
                          zone_awareness_config must be set to 3.
                        type: boolean
                    type: object
                  cognitoOptions:
                    description: Configuration block for authenticating dashboard
                      with Cognito. Detailed below.
                    properties:
                      enabled:
                        description: Whether Amazon Cognito authentication with Dashboard
                          is enabled or not. Default is false.
                        type: boolean
                      identityPoolId:
                        description: ID of the Cognito Identity Pool to use.
                        type: string
                      roleArn:
                        description: ARN of the IAM role that has the AmazonOpenSearchServiceCognitoAccess
                          policy attached.
                        type: string
                      userPoolId:
                        description: ID of the Cognito User Pool to use.
                        type: string
                    type: object
                  domainEndpointOptions:
                    description: Configuration block for domain endpoint HTTP(S) related
                      options. Detailed below.
                    properties:
                      customEndpoint:
                        description: Fully qualified domain for your custom endpoint.
                        type: string
                      customEndpointCertificateArn:
                        description: ACM certificate ARN for your custom endpoint.
                        type: string
                      customEndpointEnabled:
                        description: Whether to enable custom endpoint for the OpenSearch
                          domain.
                        type: boolean
                      enforceHttps:
                        description: Whether or not to require HTTPS. Defaults to
                          true.
                        type: boolean
                      tlsSecurityPolicy:
                        description: Name of the TLS security policy that needs to
                          be applied to the HTTPS endpoint. For valid values, refer
                          to the AWS documentation.
                        type: string
                    type: object
                  domainName:
                    description: Name of the domain.
                    type: string
                  ebsOptions:
                    description: Configuration block for EBS related options, may
                      be required based on chosen instance size. Detailed below.
                    properties:
                      ebsEnabled:
                        description: Whether EBS volumes are attached to data nodes
                          in the domain.
                        type: boolean
                      iops:
                        description: Baseline input/output (I/O) performance of EBS
                          volumes attached to data nodes. Applicable only for the
                          GP3 and Provisioned IOPS EBS volume types.
                        type: number
                      throughput:
                        description: Specifies the throughput (in MiB/s) of the EBS
                          volumes attached to data nodes. Applicable only for the
                          gp3 volume type.
                        type: number
                      volumeSize:
                        description: Size of EBS volumes attached to data nodes (in
                          GiB).
                        type: number
                      volumeType:
                        description: Type of EBS volumes attached to data nodes.
                        type: string
                    type: object
                  encryptAtRest:
                    description: Configuration block for encrypt at rest options.
                      Only available for certain instance types. Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable encryption at rest. If the
                          encrypt_at_rest block is not provided then this defaults
                          to false. Enabling encryption on new domains requires an
                          engine_version of OpenSearch_X.Y or Elasticsearch_5.1 or
                          greater.
                        type: boolean
                      kmsKeyId:
                        description: KMS key ARN to encrypt the Elasticsearch domain
                          with. If not specified then it defaults to using the aws/es
                          service KMS key. Note that KMS will accept a KMS key ID
                          but will return the key ARN.
                        type: string
                      kmsKeyIdRef:
                        description: Reference to a Key in kms to populate kmsKeyId.
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          namespace:
                            description: Namespace of the referenced object
                            type: string
                          policy:
                            description: Policies for referencing.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        required:
                        - name
                        type: object
                      kmsKeyIdSelector:
                        description: Selector for a Key in kms to populate kmsKeyId.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                    type: object
                  engineVersion:
                    description: while Elasticsearch has elasticsearch_version
                    type: string
                  ipAddressType:
                    description: The IP address type for the endpoint. Valid values
                      are ipv4 and dualstack.
                    type: string
                  logPublishingOptions:
                    description: Configuration block for publishing slow and application
                      logs to CloudWatch Logs. This block can be declared multiple
                      times, for each log_type, within the same resource. Detailed
                      below.
                    items:
                      properties:
                        cloudwatchLogGroupArn:
                          description: ARN of the Cloudwatch log group to which log
                            needs to be published.
                          type: string
                        cloudwatchLogGroupArnRef:
                          description: Reference to a Group in cloudwatchlogs to populate
                            cloudwatchLogGroupArn.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        cloudwatchLogGroupArnSelector:
                          description: Selector for a Group in cloudwatchlogs to populate
                            cloudwatchLogGroupArn.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        enabled:
                          description: Whether given log publishing option is enabled
                            or not.
                          type: boolean
                        logType:
                          description: 'Type of OpenSearch log. Valid values: INDEX_SLOW_LOGS,
                            SEARCH_SLOW_LOGS, ES_APPLICATION_LOGS, AUDIT_LOGS.'
                          type: string
                      type: object
                    type: array
                  nodeToNodeEncryption:
                    description: Configuration block for node-to-node encryption options.
                      Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable node-to-node encryption. If
                          the node_to_node_encryption block is not provided then this
                          defaults to false. Enabling node-to-node encryption of a
                          new domain requires an engine_version of OpenSearch_X.Y
                          or Elasticsearch_6.0 or greater.
                        type: boolean
                    type: object
                  offPeakWindowOptions:
                    description: Configuration to add Off Peak update options. (documentation).
                      Detailed below.
                    properties:
                      enabled:
                        description: Enabled disabled toggle for off-peak update window.
                        type: boolean
                      offPeakWindow:
                        properties:
                          windowStartTime:
                            description: 10h window for updates
                            properties:
                              hours:
                                description: Starting hour of the 10-hour window for
                                  updates
                                type: number
                              minutes:
                                description: Starting minute of the 10-hour window
                                  for updates
                                type: number
                            type: object
                        type: object
                    type: object
                  snapshotOptions:
                    description: Configuration block for snapshot related options.
                      Detailed below. DEPRECATED. For domains running OpenSearch 5.3
                      and later, Amazon OpenSearch takes hourly automated snapshots,
                      making this setting irrelevant. For domains running earlier
                      versions, OpenSearch takes daily automated snapshots.
                    properties:
                      automatedSnapshotStartHour:
                        description: Hour during which the service takes an automated
                          daily snapshot of the indices in the domain.
                        type: number
                    type: object
                  softwareUpdateOptions:
                    description: Software update options for the domain. Detailed
                      below.
                    properties:
                      autoSoftwareUpdateEnabled:
                        description: Whether automatic service software updates are
                          enabled for the domain. Defaults to false.
                        type: boolean
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcOptions:
                    description: Configuration block for VPC related options. Adding
                      or removing this configuration forces a new resource (documentation).
                      Detailed below.
                    properties:
                      securityGroupIdRefs:
                        description: References to SecurityGroup in ec2 to populate
                          securityGroupIds.
                        items:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      securityGroupIdSelector:
                        description: Selector for a list of SecurityGroup in ec2 to
                          populate securityGroupIds.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      securityGroupIds:
                        description: List of VPC Security Group IDs to be applied
                          to the OpenSearch domain endpoints. If omitted, the default
                          Security Group for the VPC will be used.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      subnetIdRefs:
                        description: References to Subnet in ec2 to populate subnetIds.
                        items:
                          description: A NamespacedReference to a named object.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      subnetIdSelector:
                        description: Selector for a list of Subnet in ec2 to populate
                          subnetIds.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          namespace:
                            description: Namespace for the selector
                            type: string
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      subnetIds:
                        description: List of VPC Subnet IDs for the OpenSearch domain
                          endpoints to be created in.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.domainName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.domainName)
                || (has(self.initProvider) && has(self.initProvider.domainName))'
          status:
            description: DomainStatus defines the observed state of Domain.
            properties:
              atProvider:
                properties:
                  accessPolicies:
                    description: ', are prefaced with es: for both.'
                    type: string
                  advancedOptions:
                    additionalProperties:
                      type: string
                    description: Key-value string pairs to specify advanced configuration
                      options.
                    type: object
                    x-kubernetes-map-type: granular
                  advancedSecurityOptions:
                    description: Configuration block for fine-grained access control.
                      Detailed below.
                    properties:
                      anonymousAuthEnabled:
                        description: Whether Anonymous auth is enabled. Enables fine-grained
                          access control on an existing domain. Ignored unless advanced_security_options
                          are enabled. Can only be enabled on an existing domain.
                        type: boolean
                      enabled:
                        description: Whether advanced security is enabled.
                        type: boolean
                      internalUserDatabaseEnabled:
                        description: Whether the internal user database is enabled.
                          Default is false.
                        type: boolean
                      masterUserOptions:
                        description: Configuration block for the main user. Detailed
                          below.
                        properties:
                          masterUserArn:
                            description: ARN for the main user. Only specify if internal_user_database_enabled
                              is not set or set to false.
                            type: string
                          masterUserName:
                            description: Main user's username, which is stored in
                              the Amazon OpenSearch Service domain's internal database.
                              Only specify if internal_user_database_enabled is set
                              to true.
                            type: string
                        type: object
                    type: object
                  arn:
                    description: ARN of the domain.
                    type: string
                  autoTuneOptions:
                    description: Configuration block for the Auto-Tune options of
                      the domain. Detailed below.
                    properties:
                      desiredState:
                        description: 'Auto-Tune desired state for the domain. Valid
                          values: ENABLED or DISABLED.'
                        type: string
                      maintenanceSchedule:
                        description: Configuration block for Auto-Tune maintenance
                          windows. Can be specified multiple times for each maintenance
                          window. Detailed below.
                        items:
                          properties:
                            cronExpressionForRecurrence:
                              description: A cron expression specifying the recurrence
                                pattern for an Auto-Tune maintenance schedule.
                              type: string
                            duration:
                              description: Configuration block for the duration of
                                the Auto-Tune maintenance window. Detailed below.
                              properties:
                                unit:
                                  description: 'Unit of time specifying the duration
                                    of an Auto-Tune maintenance window. Valid values:
                                    HOURS.'
                                  type: string
                                value:
                                  description: An integer specifying the value of
                                    the duration of an Auto-Tune maintenance window.
                                  type: number
                              type: object
                            startAt:
                              description: Date and time at which to start the Auto-Tune
                                maintenance schedule in RFC3339 format.
                              type: string
                          type: object
                        type: array
                      rollbackOnDisable:
                        description: 'Whether to roll back to default Auto-Tune settings
                          when disabling Auto-Tune. Valid values: DEFAULT_ROLLBACK
                          or NO_ROLLBACK.'
                        type: string
                      useOffPeakWindow:
                        description: Whether to schedule Auto-Tune optimizations that
                          require blue/green deployments during the domain's configured
                          daily off-peak window. Defaults to false.
                        type: boolean
                    type: object
                  clusterConfig:
                    description: Configuration block for the cluster of the domain.
                      Detailed below.
                    properties:
                      coldStorageOptions:
                        description: Configuration block containing cold storage configuration.
                          Detailed below.
                        properties:
                          enabled:
                            description: Boolean to enable cold storage for an OpenSearch
                              domain. Defaults to false. Master and ultrawarm nodes
                              must be enabled for cold storage.
                            type: boolean
                        type: object
                      dedicatedMasterCount:
                        description: Number of dedicated main nodes in the cluster.
                        type: number
                      dedicatedMasterEnabled:
                        description: Whether dedicated main nodes are enabled for
                          the cluster.
                        type: boolean
                      dedicatedMasterType:
                        description: Instance type of the dedicated main nodes in
                          the cluster.
                        type: string
                      instanceCount:
                        description: Number of instances in the cluster.
                        type: number
                      instanceType:
                        description: Instance type of data nodes in the cluster.
                        type: string
                      multiAzWithStandbyEnabled:
                        description: Whether a multi-AZ domain is turned on with a
                          standby AZ. For more information, see Configuring a multi-AZ
                          domain in Amazon OpenSearch Service.
                        type: boolean
                      nodeOptions:
                        description: List of node options for the domain.
                        items:
                          properties:
                            nodeConfig:
                              description: Container to specify sizing of a node type.
                              properties:
                                count:
                                  description: Number of nodes of a particular node
                                    type in the cluster.
                                  type: number
                                enabled:
                                  description: Whether a particular node type is enabled.
                                  type: boolean
                                type:
                                  description: The instance type of a particular node
                                    type in the cluster.
                                  type: string
                              type: object
                            nodeType:
                              description: 'Type of node this configuration describes.
                                Valid values: coordinator.'
                              type: string
                          type: object
                        type: array
                      warmCount:
                        description: Number of warm nodes in the cluster. Valid values
                          are between 2 and 150. warm_count can be only and must be
                          set when warm_enabled is set to true.
                        type: number
                      warmEnabled:
                        description: Whether to enable warm storage.
                        type: boolean
                      warmType:
                        description: Instance type for the OpenSearch cluster's warm
                          nodes. Valid values are ultrawarm1.medium.search, ultrawarm1.large.search
                          and ultrawarm1.xlarge.search. warm_type can be only and
                          must be set when warm_enabled is set to true.
                        type: string
                      zoneAwarenessConfig:
                        description: Configuration block containing zone awareness
                          settings. Detailed below.
                        properties:
                          availabilityZoneCount:
                            description: 'Number of Availability Zones for the domain
                              to use with zone_awareness_enabled. Defaults to 2. Valid
                              values: 2 or 3.'
                            type: number
                        type: object
                      zoneAwarenessEnabled:
                        description: Whether zone awareness is enabled, set to true
                          for multi-az deployment. To enable awareness with three
                          Availability Zones, the availability_zone_count within the
                          zone_awareness_config must be set to 3.
                        type: boolean
                    type: object
                  cognitoOptions:
                    description: Configuration block for authenticating dashboard
                      with Cognito. Detailed below.
                    properties:
                      enabled:
                        description: Whether Amazon Cognito authentication with Dashboard
                          is enabled or not. Default is false.
                        type: boolean
                      identityPoolId:
                        description: ID of the Cognito Identity Pool to use.
                        type: string
                      roleArn:
                        description: ARN of the IAM role that has the AmazonOpenSearchServiceCognitoAccess
                          policy attached.
                        type: string
                      userPoolId:
                        description: ID of the Cognito User Pool to use.
                        type: string
                    type: object
                  dashboardEndpoint:
                    description: Domain-specific endpoint for Dashboard without https
                      scheme.
                    type: string
                  dashboardEndpointV2:
                    description: V2 domain endpoint for Dashboard that works with
                      both IPv4 and IPv6 addresses, without https scheme.
                    type: string
                  domainEndpointOptions:
                    description: Configuration block for domain endpoint HTTP(S) related
                      options. Detailed below.
                    properties:
                      customEndpoint:
                        description: Fully qualified domain for your custom endpoint.
                        type: string
                      customEndpointCertificateArn:
                        description: ACM certificate ARN for your custom endpoint.
                        type: string
                      customEndpointEnabled:
                        description: Whether to enable custom endpoint for the OpenSearch
                          domain.
                        type: boolean
                      enforceHttps:
                        description: Whether or not to require HTTPS. Defaults to
                          true.
                        type: boolean
                      tlsSecurityPolicy:
                        description: Name of the TLS security policy that needs to
                          be applied to the HTTPS endpoint. For valid values, refer
                          to the AWS documentation.
                        type: string
                    type: object
                  domainEndpointV2HostedZoneId:
                    description: Dual stack hosted zone ID for the domain.
                    type: string
                  domainId:
                    description: Unique identifier for the domain.
                    type: string
                  domainName:
                    description: Name of the domain.
                    type: string
                  ebsOptions:
                    description: Configuration block for EBS related options, may
                      be required based on chosen instance size. Detailed below.
                    properties:
                      ebsEnabled:
                        description: Whether EBS volumes are attached to data nodes
                          in the domain.
                        type: boolean
                      iops:
                        description: Baseline input/output (I/O) performance of EBS
                          volumes attached to data nodes. Applicable only for the
                          GP3 and Provisioned IOPS EBS volume types.
                        type: number
                      throughput:
                        description: Specifies the throughput (in MiB/s) of the EBS
                          volumes attached to data nodes. Applicable only for the
                          gp3 volume type.
                        type: number
                      volumeSize:
                        description: Size of EBS volumes attached to data nodes (in
                          GiB).
                        type: number
                      volumeType:
                        description: Type of EBS volumes attached to data nodes.
                        type: string
                    type: object
                  encryptAtRest:
                    description: Configuration block for encrypt at rest options.
                      Only available for certain instance types. Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable encryption at rest. If the
                          encrypt_at_rest block is not provided then this defaults
                          to false. Enabling encryption on new domains requires an
                          engine_version of OpenSearch_X.Y or Elasticsearch_5.1 or
                          greater.
                        type: boolean
                      kmsKeyId:
                        description: KMS key ARN to encrypt the Elasticsearch domain
                          with. If not specified then it defaults to using the aws/es
                          service KMS key. Note that KMS will accept a KMS key ID
                          but will return the key ARN.
                        type: string
                    type: object
                  endpoint:
                    description: Domain-specific endpoint used to submit index, search,
                      and data upload requests.
                    type: string
                  endpointV2:
                    description: V2 domain endpoint that works with both IPv4 and
                      IPv6 addresses, used to submit index, search, and data upload
                      requests.
                    type: string
                  engineVersion:
                    description: while Elasticsearch has elasticsearch_version
                    type: string
                  id:
                    type: string
                  ipAddressType:
                    description: The IP address type for the endpoint. Valid values
                      are ipv4 and dualstack.
                    type: string
                  logPublishingOptions:
                    description: Configuration block for publishing slow and application
                      logs to CloudWatch Logs. This block can be declared multiple
                      times, for each log_type, within the same resource. Detailed
                      below.
                    items:
                      properties:
                        cloudwatchLogGroupArn:
                          description: ARN of the Cloudwatch log group to which log
                            needs to be published.
                          type: string
                        enabled:
                          description: Whether given log publishing option is enabled
                            or not.
                          type: boolean
                        logType:
                          description: 'Type of OpenSearch log. Valid values: INDEX_SLOW_LOGS,
                            SEARCH_SLOW_LOGS, ES_APPLICATION_LOGS, AUDIT_LOGS.'
                          type: string
                      type: object
                    type: array
                  nodeToNodeEncryption:
                    description: Configuration block for node-to-node encryption options.
                      Detailed below.
                    properties:
                      enabled:
                        description: Whether to enable node-to-node encryption. If
                          the node_to_node_encryption block is not provided then this
                          defaults to false. Enabling node-to-node encryption of a
                          new domain requires an engine_version of OpenSearch_X.Y
                          or Elasticsearch_6.0 or greater.
                        type: boolean
                    type: object
                  offPeakWindowOptions:
                    description: Configuration to add Off Peak update options. (documentation).
                      Detailed below.
                    properties:
                      enabled:
                        description: Enabled disabled toggle for off-peak update window.
                        type: boolean
                      offPeakWindow:
                        properties:
                          windowStartTime:
                            description: 10h window for updates
                            properties:
                              hours:
                                description: Starting hour of the 10-hour window for
                                  updates
                                type: number
                              minutes:
                                description: Starting minute of the 10-hour window
                                  for updates
                                type: number
                            type: object
                        type: object
                    type: object
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  snapshotOptions:
                    description: Configuration block for snapshot related options.
                      Detailed below. DEPRECATED. For domains running OpenSearch 5.3
                      and later, Amazon OpenSearch takes hourly automated snapshots,
                      making this setting irrelevant. For domains running earlier
                      versions, OpenSearch takes daily automated snapshots.
                    properties:
                      automatedSnapshotStartHour:
                        description: Hour during which the service takes an automated
                          daily snapshot of the indices in the domain.
                        type: number
                    type: object
                  softwareUpdateOptions:
                    description: Software update options for the domain. Detailed
                      below.
                    properties:
                      autoSoftwareUpdateEnabled:
                        description: Whether automatic service software updates are
                          enabled for the domain. Defaults to false.
                        type: boolean
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: Map of tags assigned to the resource, including those
                      inherited from the provider default_tags configuration block.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcOptions:
                    description: Configuration block for VPC related options. Adding
                      or removing this configuration forces a new resource (documentation).
                      Detailed below.
                    properties:
                      availabilityZones:
                        description: If the domain was created inside a VPC, the names
                          of the availability zones the configured subnet_ids were
                          created inside.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      securityGroupIds:
                        description: List of VPC Security Group IDs to be applied
                          to the OpenSearch domain endpoints. If omitted, the default
                          Security Group for the VPC will be used.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      subnetIds:
                        description: List of VPC Subnet IDs for the OpenSearch domain
                          endpoints to be created in.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      vpcId:
                        description: If the domain was created inside a VPC, the ID
                          of the VPC.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
