---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: onlineevaluationconfigs.bedrockagentcore.aws.upbound.io
spec:
  group: bedrockagentcore.aws.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: OnlineEvaluationConfig
    listKind: OnlineEvaluationConfigList
    plural: onlineevaluationconfigs
    singular: onlineevaluationconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OnlineEvaluationConfig is the Schema for the OnlineEvaluationConfigs
          API. Manages an AWS Bedrock AgentCore Online Evaluation Configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OnlineEvaluationConfigSpec defines the desired state of OnlineEvaluationConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dataSourceConfig:
                    description: Data source configuration specifying where to read
                      agent traces. See data_source_config Block below.
                    properties:
                      cloudwatchLogs:
                        description: CloudWatch logs configuration for reading agent
                          traces. See cloudwatch_logs Block below.
                        properties:
                          logGroupNames:
                            description: List of CloudWatch log group names to monitor
                              for agent traces. Maximum 5.
                            items:
                              type: string
                            type: array
                          logGroupNamesRefs:
                            description: References to Group in cloudwatchlogs to
                              populate logGroupNames.
                            items:
                              description: A Reference to a named object.
                              properties:
                                name:
                                  description: Name of the referenced object.
                                  type: string
                                policy:
                                  description: Policies for referencing.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          logGroupNamesSelector:
                            description: Selector for a list of Group in cloudwatchlogs
                              to populate logGroupNames.
                            properties:
                              matchControllerRef:
                                description: |-
                                  MatchControllerRef ensures an object with the same controller reference
                                  as the selecting object is selected.
                                type: boolean
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: MatchLabels ensures an object with matching
                                  labels is selected.
                                type: object
                              policy:
                                description: Policies for selection.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            type: object
                          serviceNames:
                            description: List of service names to filter traces within
                              the specified log groups.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  description:
                    description: Description of the online evaluation configuration.
                    type: string
                  enableOnCreate:
                    description: Whether to enable the online evaluation configuration
                      immediately upon creation.
                    type: boolean
                  evaluationExecutionRoleArn:
                    description: ARN of the IAM role that grants permissions to read
                      from CloudWatch logs, write evaluation results, and invoke Amazon
                      Bedrock models for evaluation.
                    type: string
                  evaluationExecutionRoleArnRef:
                    description: Reference to a Role in iam to populate evaluationExecutionRoleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  evaluationExecutionRoleArnSelector:
                    description: Selector for a Role in iam to populate evaluationExecutionRoleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  evaluator:
                    description: List of evaluators to apply during online evaluation.
                      Minimum 1, maximum 10. See evaluator Block below.
                    items:
                      properties:
                        evaluatorId:
                          description: Unique identifier of the evaluator. Can reference
                            builtin evaluators (e.g., Builtin.Helpfulness, Builtin.GoalSuccessRate)
                            or custom evaluator IDs.
                          type: string
                        evaluatorIdRef:
                          description: Reference to a Evaluator in bedrockagentcore
                            to populate evaluatorId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        evaluatorIdSelector:
                          description: Selector for a Evaluator in bedrockagentcore
                            to populate evaluatorId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  executionStatus:
                    description: 'Execution status to enable or disable the online
                      evaluation. Valid values: ENABLED, DISABLED. Computed on create
                      based on enable_on_create.'
                    type: string
                  onlineEvaluationConfigName:
                    description: Name of the online evaluation configuration. Must
                      start with a letter and contain only alphanumeric characters
                      and underscores, up to 48 characters.
                    type: string
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  rule:
                    description: Evaluation rule defining sampling configuration,
                      filters, and session detection settings. See rule Block below.
                    properties:
                      filter:
                        description: List of filters determining which agent traces
                          to evaluate. Maximum 5. See filter Block below.
                        items:
                          properties:
                            key:
                              description: Key or field name to filter on within the
                                agent trace data.
                              type: string
                            operator:
                              description: 'Comparison operator. Valid values: Equals,
                                NotEquals, GreaterThan, LessThan, GreaterThanOrEqual,
                                LessThanOrEqual, Contains, NotContains.'
                              type: string
                            value:
                              description: Value to compare against. See value Block
                                below.
                              properties:
                                booleanValue:
                                  description: Boolean value for true/false filtering.
                                  type: boolean
                                doubleValue:
                                  description: Numeric value for numerical filtering.
                                  type: number
                                stringValue:
                                  description: String value for text-based filtering.
                                  type: string
                              type: object
                          type: object
                        type: array
                      samplingConfig:
                        description: Sampling configuration determining what percentage
                          of agent traces to evaluate. See sampling_config Block below.
                        properties:
                          samplingPercentage:
                            description: Percentage of agent traces to sample for
                              evaluation, from 0.01 to 100.
                            type: number
                        type: object
                      sessionConfig:
                        description: Session configuration defining timeout settings
                          for detecting when agent sessions are complete. See session_config
                          Block below.
                        properties:
                          sessionTimeoutMinutes:
                            description: Minutes of inactivity after which a session
                              is considered complete. Between 1 and 60.
                            type: number
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                required:
                - region
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceConfig:
                    description: Data source configuration specifying where to read
                      agent traces. See data_source_config Block below.
                    properties:
                      cloudwatchLogs:
                        description: CloudWatch logs configuration for reading agent
                          traces. See cloudwatch_logs Block below.
                        properties:
                          logGroupNames:
                            description: List of CloudWatch log group names to monitor
                              for agent traces. Maximum 5.
                            items:
                              type: string
                            type: array
                          logGroupNamesRefs:
                            description: References to Group in cloudwatchlogs to
                              populate logGroupNames.
                            items:
                              description: A Reference to a named object.
                              properties:
                                name:
                                  description: Name of the referenced object.
                                  type: string
                                policy:
                                  description: Policies for referencing.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          logGroupNamesSelector:
                            description: Selector for a list of Group in cloudwatchlogs
                              to populate logGroupNames.
                            properties:
                              matchControllerRef:
                                description: |-
                                  MatchControllerRef ensures an object with the same controller reference
                                  as the selecting object is selected.
                                type: boolean
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: MatchLabels ensures an object with matching
                                  labels is selected.
                                type: object
                              policy:
                                description: Policies for selection.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            type: object
                          serviceNames:
                            description: List of service names to filter traces within
                              the specified log groups.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  description:
                    description: Description of the online evaluation configuration.
                    type: string
                  enableOnCreate:
                    description: Whether to enable the online evaluation configuration
                      immediately upon creation.
                    type: boolean
                  evaluationExecutionRoleArn:
                    description: ARN of the IAM role that grants permissions to read
                      from CloudWatch logs, write evaluation results, and invoke Amazon
                      Bedrock models for evaluation.
                    type: string
                  evaluationExecutionRoleArnRef:
                    description: Reference to a Role in iam to populate evaluationExecutionRoleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  evaluationExecutionRoleArnSelector:
                    description: Selector for a Role in iam to populate evaluationExecutionRoleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  evaluator:
                    description: List of evaluators to apply during online evaluation.
                      Minimum 1, maximum 10. See evaluator Block below.
                    items:
                      properties:
                        evaluatorId:
                          description: Unique identifier of the evaluator. Can reference
                            builtin evaluators (e.g., Builtin.Helpfulness, Builtin.GoalSuccessRate)
                            or custom evaluator IDs.
                          type: string
                        evaluatorIdRef:
                          description: Reference to a Evaluator in bedrockagentcore
                            to populate evaluatorId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        evaluatorIdSelector:
                          description: Selector for a Evaluator in bedrockagentcore
                            to populate evaluatorId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  executionStatus:
                    description: 'Execution status to enable or disable the online
                      evaluation. Valid values: ENABLED, DISABLED. Computed on create
                      based on enable_on_create.'
                    type: string
                  onlineEvaluationConfigName:
                    description: Name of the online evaluation configuration. Must
                      start with a letter and contain only alphanumeric characters
                      and underscores, up to 48 characters.
                    type: string
                  rule:
                    description: Evaluation rule defining sampling configuration,
                      filters, and session detection settings. See rule Block below.
                    properties:
                      filter:
                        description: List of filters determining which agent traces
                          to evaluate. Maximum 5. See filter Block below.
                        items:
                          properties:
                            key:
                              description: Key or field name to filter on within the
                                agent trace data.
                              type: string
                            operator:
                              description: 'Comparison operator. Valid values: Equals,
                                NotEquals, GreaterThan, LessThan, GreaterThanOrEqual,
                                LessThanOrEqual, Contains, NotContains.'
                              type: string
                            value:
                              description: Value to compare against. See value Block
                                below.
                              properties:
                                booleanValue:
                                  description: Boolean value for true/false filtering.
                                  type: boolean
                                doubleValue:
                                  description: Numeric value for numerical filtering.
                                  type: number
                                stringValue:
                                  description: String value for text-based filtering.
                                  type: string
                              type: object
                          type: object
                        type: array
                      samplingConfig:
                        description: Sampling configuration determining what percentage
                          of agent traces to evaluate. See sampling_config Block below.
                        properties:
                          samplingPercentage:
                            description: Percentage of agent traces to sample for
                              evaluation, from 0.01 to 100.
                            type: number
                        type: object
                      sessionConfig:
                        description: Session configuration defining timeout settings
                          for detecting when agent sessions are complete. See session_config
                          Block below.
                        properties:
                          sessionTimeoutMinutes:
                            description: Minutes of inactivity after which a session
                              is considered complete. Between 1 and 60.
                            type: number
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enableOnCreate is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enableOnCreate)
                || (has(self.initProvider) && has(self.initProvider.enableOnCreate))'
            - message: spec.forProvider.onlineEvaluationConfigName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.onlineEvaluationConfigName)
                || (has(self.initProvider) && has(self.initProvider.onlineEvaluationConfigName))'
          status:
            description: OnlineEvaluationConfigStatus defines the observed state of
              OnlineEvaluationConfig.
            properties:
              atProvider:
                properties:
                  dataSourceConfig:
                    description: Data source configuration specifying where to read
                      agent traces. See data_source_config Block below.
                    properties:
                      cloudwatchLogs:
                        description: CloudWatch logs configuration for reading agent
                          traces. See cloudwatch_logs Block below.
                        properties:
                          logGroupNames:
                            description: List of CloudWatch log group names to monitor
                              for agent traces. Maximum 5.
                            items:
                              type: string
                            type: array
                          serviceNames:
                            description: List of service names to filter traces within
                              the specified log groups.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  description:
                    description: Description of the online evaluation configuration.
                    type: string
                  enableOnCreate:
                    description: Whether to enable the online evaluation configuration
                      immediately upon creation.
                    type: boolean
                  evaluationExecutionRoleArn:
                    description: ARN of the IAM role that grants permissions to read
                      from CloudWatch logs, write evaluation results, and invoke Amazon
                      Bedrock models for evaluation.
                    type: string
                  evaluator:
                    description: List of evaluators to apply during online evaluation.
                      Minimum 1, maximum 10. See evaluator Block below.
                    items:
                      properties:
                        evaluatorId:
                          description: Unique identifier of the evaluator. Can reference
                            builtin evaluators (e.g., Builtin.Helpfulness, Builtin.GoalSuccessRate)
                            or custom evaluator IDs.
                          type: string
                      type: object
                    type: array
                  executionStatus:
                    description: 'Execution status to enable or disable the online
                      evaluation. Valid values: ENABLED, DISABLED. Computed on create
                      based on enable_on_create.'
                    type: string
                  id:
                    type: string
                  onlineEvaluationConfigArn:
                    description: ARN of the online evaluation configuration.
                    type: string
                  onlineEvaluationConfigId:
                    description: Unique identifier of the online evaluation configuration.
                    type: string
                  onlineEvaluationConfigName:
                    description: Name of the online evaluation configuration. Must
                      start with a letter and contain only alphanumeric characters
                      and underscores, up to 48 characters.
                    type: string
                  outputConfig:
                    description: Configuration specifying where evaluation results
                      are written. See output_config Block below.
                    items:
                      properties:
                        cloudwatchConfig:
                          description: CloudWatch configuration for evaluation results.
                            See cloudwatch_config Block below.
                          items:
                            properties:
                              logGroupName:
                                description: Name of the CloudWatch log group where
                                  evaluation results are written.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  rule:
                    description: Evaluation rule defining sampling configuration,
                      filters, and session detection settings. See rule Block below.
                    properties:
                      filter:
                        description: List of filters determining which agent traces
                          to evaluate. Maximum 5. See filter Block below.
                        items:
                          properties:
                            key:
                              description: Key or field name to filter on within the
                                agent trace data.
                              type: string
                            operator:
                              description: 'Comparison operator. Valid values: Equals,
                                NotEquals, GreaterThan, LessThan, GreaterThanOrEqual,
                                LessThanOrEqual, Contains, NotContains.'
                              type: string
                            value:
                              description: Value to compare against. See value Block
                                below.
                              properties:
                                booleanValue:
                                  description: Boolean value for true/false filtering.
                                  type: boolean
                                doubleValue:
                                  description: Numeric value for numerical filtering.
                                  type: number
                                stringValue:
                                  description: String value for text-based filtering.
                                  type: string
                              type: object
                          type: object
                        type: array
                      samplingConfig:
                        description: Sampling configuration determining what percentage
                          of agent traces to evaluate. See sampling_config Block below.
                        properties:
                          samplingPercentage:
                            description: Percentage of agent traces to sample for
                              evaluation, from 0.01 to 100.
                            type: number
                        type: object
                      sessionConfig:
                        description: Session configuration defining timeout settings
                          for detecting when agent sessions are complete. See session_config
                          Block below.
                        properties:
                          sessionTimeoutMinutes:
                            description: Minutes of inactivity after which a session
                              is considered complete. Between 1 and 60.
                            type: number
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: Map of tags assigned to the resource, including those
                      inherited from the provider default_tags configuration block.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
