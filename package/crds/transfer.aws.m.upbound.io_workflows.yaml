---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workflows.transfer.aws.m.upbound.io
spec:
  group: transfer.aws.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the Workflows API. Provides a AWS
          Transfer Workflow resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow
            properties:
              forProvider:
                properties:
                  description:
                    description: Textual description for the workflow.
                    type: string
                  onExceptionSteps:
                    description: Steps (actions) to take if errors are encountered
                      during execution of the workflow. See on_exception_steps Block
                      below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  steps:
                    description: Details for the steps that are in the specified workflow.
                      See steps Block below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            targetRef:
                              description: Reference to a Function in lambda to populate
                                target.
                              properties:
                                name:
                                  description: Name of the referenced object.
                                  type: string
                                namespace:
                                  description: Namespace of the referenced object
                                  type: string
                                policy:
                                  description: Policies for referencing.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              required:
                              - name
                              type: object
                            targetSelector:
                              description: Selector for a Function in lambda to populate
                                target.
                              properties:
                                matchControllerRef:
                                  description: |-
                                    MatchControllerRef ensures an object with the same controller reference
                                    as the selecting object is selected.
                                  type: boolean
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: MatchLabels ensures an object with
                                    matching labels is selected.
                                  type: object
                                namespace:
                                  description: Namespace for the selector
                                  type: string
                                policy:
                                  description: Policies for selection.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              type: object
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                required:
                - region
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: Textual description for the workflow.
                    type: string
                  onExceptionSteps:
                    description: Steps (actions) to take if errors are encountered
                      during execution of the workflow. See on_exception_steps Block
                      below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  steps:
                    description: Details for the steps that are in the specified workflow.
                      See steps Block below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            targetRef:
                              description: Reference to a Function in lambda to populate
                                target.
                              properties:
                                name:
                                  description: Name of the referenced object.
                                  type: string
                                namespace:
                                  description: Namespace of the referenced object
                                  type: string
                                policy:
                                  description: Policies for referencing.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              required:
                              - name
                              type: object
                            targetSelector:
                              description: Selector for a Function in lambda to populate
                                target.
                              properties:
                                matchControllerRef:
                                  description: |-
                                    MatchControllerRef ensures an object with the same controller reference
                                    as the selecting object is selected.
                                  type: boolean
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: MatchLabels ensures an object with
                                    matching labels is selected.
                                  type: object
                                namespace:
                                  description: Namespace for the selector
                                  type: string
                                policy:
                                  description: Policies for selection.
                                  properties:
                                    resolution:
                                      default: Required
                                      description: |-
                                        Resolution specifies whether resolution of this reference is required.
                                        The default is 'Required', which means the reconcile will fail if the
                                        reference cannot be resolved. 'Optional' means this reference will be
                                        a no-op if it cannot be resolved.
                                      enum:
                                      - Required
                                      - Optional
                                      type: string
                                    resolve:
                                      description: |-
                                        Resolve specifies when this reference should be resolved. The default
                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                        the corresponding field is not present. Use 'Always' to resolve the
                                        reference on every reconcile.
                                      enum:
                                      - Always
                                      - IfNotPresent
                                      type: string
                                  type: object
                              type: object
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.steps is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.steps)
                || (has(self.initProvider) && has(self.initProvider.steps))'
          status:
            description: WorkflowStatus defines the observed state of Workflow.
            properties:
              atProvider:
                properties:
                  arn:
                    description: Workflow ARN.
                    type: string
                  description:
                    description: Textual description for the workflow.
                    type: string
                  id:
                    description: Workflow ID.
                    type: string
                  onExceptionSteps:
                    description: Steps (actions) to take if errors are encountered
                      during execution of the workflow. See on_exception_steps Block
                      below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  steps:
                    description: Details for the steps that are in the specified workflow.
                      See steps Block below.
                    items:
                      properties:
                        copyStepDetails:
                          description: Details for a step that performs a file copy.
                            See copy_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        customStepDetails:
                          description: Details for a step that invokes a lambda function.
                            See custom_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            target:
                              description: ARN for the lambda function that is being
                                called.
                              type: string
                            timeoutSeconds:
                              description: Timeout, in seconds, for the step.
                              type: number
                          type: object
                        decryptStepDetails:
                          description: Details for a step that decrypts the file.
                            See decrypt_step_details Block below.
                          properties:
                            destinationFileLocation:
                              description: Location for the file being copied. Use
                                ${Transfer:username} in this field to parametrize
                                the destination prefix by username. See destination_file_location
                                Block below.
                              properties:
                                efsFileLocation:
                                  description: Details for the EFS file being copied.
                                    See efs_file_location Block below.
                                  properties:
                                    fileSystemId:
                                      description: ID of the file system, assigned
                                        by Amazon EFS.
                                      type: string
                                    path:
                                      description: Pathname for the folder being used
                                        by a workflow.
                                      type: string
                                  type: object
                                s3FileLocation:
                                  description: Details for the S3 file being copied.
                                    See s3_file_location Block below.
                                  properties:
                                    bucket:
                                      description: S3 bucket for the customer input
                                        file.
                                      type: string
                                    key:
                                      description: Name assigned to the tag that you
                                        create.
                                      type: string
                                  type: object
                              type: object
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            overwriteExisting:
                              description: Flag that indicates whether or not to overwrite
                                an existing file of the same name. The default is
                                FALSE. Valid values are TRUE and FALSE.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            type:
                              description: Type of encryption used. Currently, this
                                value must be "PGP".
                              type: string
                          type: object
                        deleteStepDetails:
                          description: Details for a step that deletes the file. See
                            delete_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                          type: object
                        tagStepDetails:
                          description: Details for a step that creates one or more
                            tags. See tag_step_details Block below.
                          properties:
                            name:
                              description: Name of the step, used as an identifier.
                              type: string
                            sourceFileLocation:
                              description: 'File to use as input to the workflow step:
                                either the output from the previous step, or the originally
                                uploaded file for the workflow. Enter ${previous.file}
                                to use the previous file as the input. In this case,
                                this workflow step uses the output file from the previous
                                workflow step as input. This is the default value.
                                Enter ${original.file} to use the originally-uploaded
                                file location as input for this step.'
                              type: string
                            tags:
                              description: Key-value map of resource tags.
                              items:
                                properties:
                                  key:
                                    description: Name assigned to the tag that you
                                      create.
                                    type: string
                                  value:
                                    description: Value that corresponds to the key.
                                    type: string
                                type: object
                              type: array
                          type: object
                        type:
                          description: Step type. Valid values are COPY, CUSTOM, DECRYPT,
                            DELETE, and TAG.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: Map of tags assigned to the resource, including those
                      inherited from the provider default_tags configuration block.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
