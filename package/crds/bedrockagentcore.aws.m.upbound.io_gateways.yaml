---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gateways.bedrockagentcore.aws.m.upbound.io
spec:
  group: bedrockagentcore.aws.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: Gateway
    listKind: GatewayList
    plural: gateways
    singular: gateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Gateway is the Schema for the Gateways API. Manages an AWS Bedrock
          AgentCore Gateway.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GatewaySpec defines the desired state of Gateway
            properties:
              forProvider:
                properties:
                  authorizerConfiguration:
                    description: Configuration for request authorization. Required
                      when authorizer_type is set to CUSTOM_JWT. See authorizer_configuration
                      below.
                    properties:
                      customJwtAuthorizer:
                        description: JWT-based authorization configuration block.
                          See custom_jwt_authorizer below.
                        properties:
                          allowedAudience:
                            description: Set of allowed audience values for JWT token
                              validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          allowedClients:
                            description: Set of allowed client IDs for JWT token validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          discoveryUrl:
                            description: URL used to fetch OpenID Connect configuration
                              or authorization server metadata. Must end with .well-known/openid-configuration.
                            type: string
                        type: object
                    type: object
                  authorizerType:
                    description: 'Type of authorizer to use. Valid values: CUSTOM_JWT,
                      AWS_IAM. When set to CUSTOM_JWT, authorizer_configuration block
                      is required.'
                    type: string
                  description:
                    description: Description of the gateway.
                    type: string
                  exceptionLevel:
                    description: 'Exception level for the gateway. Valid values: INFO,
                      WARN, ERROR.'
                    type: string
                  interceptorConfiguration:
                    description: List of interceptor configurations for the gateway.
                      Minimum of 1, maximum of 2. See interceptor_configuration below.
                    items:
                      properties:
                        inputConfiguration:
                          description: Input configuration for the interceptor. See
                            input_configuration below.
                          properties:
                            passRequestHeaders:
                              description: Whether to pass request headers to the
                                interceptor.
                              type: boolean
                          type: object
                        interceptionPoints:
                          description: 'Set of interception points. Valid values:
                            REQUEST, RESPONSE.'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        interceptor:
                          description: Interceptor infrastructure configuration. See
                            interceptor below.
                          properties:
                            lambda:
                              description: Lambda function configuration for the interceptor.
                                See lambda below.
                              properties:
                                arn:
                                  description: ARN of the Lambda function to invoke
                                    for the interceptor.
                                  type: string
                                arnRef:
                                  description: Reference to a Function in lambda to
                                    populate arn.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                arnSelector:
                                  description: Selector for a Function in lambda to
                                    populate arn.
                                  properties:
                                    matchControllerRef:
                                      description: |-
                                        MatchControllerRef ensures an object with the same controller reference
                                        as the selecting object is selected.
                                      type: boolean
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: MatchLabels ensures an object with
                                        matching labels is selected.
                                      type: object
                                    namespace:
                                      description: Namespace for the selector
                                      type: string
                                    policy:
                                      description: Policies for selection.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  type: object
                              type: object
                          type: object
                      type: object
                    type: array
                  kmsKeyArn:
                    description: ARN of the KMS key used to encrypt the gateway data.
                    type: string
                  kmsKeyArnRef:
                    description: Reference to a Key in kms to populate kmsKeyArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  kmsKeyArnSelector:
                    description: Selector for a Key in kms to populate kmsKeyArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: Name of the gateway.
                    type: string
                  protocolConfiguration:
                    description: Protocol-specific configuration for the gateway.
                      See protocol_configuration below.
                    properties:
                      mcp:
                        description: Model Context Protocol (MCP) configuration block.
                          See mcp below.
                        properties:
                          instructions:
                            description: Instructions for the MCP protocol configuration.
                            type: string
                          searchType:
                            description: 'Search type for MCP. Valid values: SEMANTIC.'
                            type: string
                          supportedVersions:
                            description: Set of supported MCP protocol versions.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  protocolType:
                    description: 'Protocol type for the gateway. Valid values: MCP.'
                    type: string
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  roleArn:
                    description: ARN of the IAM role that the gateway assumes to access
                      AWS services.
                    type: string
                  roleArnRef:
                    description: Reference to a Role in iam to populate roleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleArnSelector:
                    description: Selector for a Role in iam to populate roleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                required:
                - region
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizerConfiguration:
                    description: Configuration for request authorization. Required
                      when authorizer_type is set to CUSTOM_JWT. See authorizer_configuration
                      below.
                    properties:
                      customJwtAuthorizer:
                        description: JWT-based authorization configuration block.
                          See custom_jwt_authorizer below.
                        properties:
                          allowedAudience:
                            description: Set of allowed audience values for JWT token
                              validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          allowedClients:
                            description: Set of allowed client IDs for JWT token validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          discoveryUrl:
                            description: URL used to fetch OpenID Connect configuration
                              or authorization server metadata. Must end with .well-known/openid-configuration.
                            type: string
                        type: object
                    type: object
                  authorizerType:
                    description: 'Type of authorizer to use. Valid values: CUSTOM_JWT,
                      AWS_IAM. When set to CUSTOM_JWT, authorizer_configuration block
                      is required.'
                    type: string
                  description:
                    description: Description of the gateway.
                    type: string
                  exceptionLevel:
                    description: 'Exception level for the gateway. Valid values: INFO,
                      WARN, ERROR.'
                    type: string
                  interceptorConfiguration:
                    description: List of interceptor configurations for the gateway.
                      Minimum of 1, maximum of 2. See interceptor_configuration below.
                    items:
                      properties:
                        inputConfiguration:
                          description: Input configuration for the interceptor. See
                            input_configuration below.
                          properties:
                            passRequestHeaders:
                              description: Whether to pass request headers to the
                                interceptor.
                              type: boolean
                          type: object
                        interceptionPoints:
                          description: 'Set of interception points. Valid values:
                            REQUEST, RESPONSE.'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        interceptor:
                          description: Interceptor infrastructure configuration. See
                            interceptor below.
                          properties:
                            lambda:
                              description: Lambda function configuration for the interceptor.
                                See lambda below.
                              properties:
                                arn:
                                  description: ARN of the Lambda function to invoke
                                    for the interceptor.
                                  type: string
                                arnRef:
                                  description: Reference to a Function in lambda to
                                    populate arn.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                arnSelector:
                                  description: Selector for a Function in lambda to
                                    populate arn.
                                  properties:
                                    matchControllerRef:
                                      description: |-
                                        MatchControllerRef ensures an object with the same controller reference
                                        as the selecting object is selected.
                                      type: boolean
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: MatchLabels ensures an object with
                                        matching labels is selected.
                                      type: object
                                    namespace:
                                      description: Namespace for the selector
                                      type: string
                                    policy:
                                      description: Policies for selection.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  type: object
                              type: object
                          type: object
                      type: object
                    type: array
                  kmsKeyArn:
                    description: ARN of the KMS key used to encrypt the gateway data.
                    type: string
                  kmsKeyArnRef:
                    description: Reference to a Key in kms to populate kmsKeyArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  kmsKeyArnSelector:
                    description: Selector for a Key in kms to populate kmsKeyArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: Name of the gateway.
                    type: string
                  protocolConfiguration:
                    description: Protocol-specific configuration for the gateway.
                      See protocol_configuration below.
                    properties:
                      mcp:
                        description: Model Context Protocol (MCP) configuration block.
                          See mcp below.
                        properties:
                          instructions:
                            description: Instructions for the MCP protocol configuration.
                            type: string
                          searchType:
                            description: 'Search type for MCP. Valid values: SEMANTIC.'
                            type: string
                          supportedVersions:
                            description: Set of supported MCP protocol versions.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  protocolType:
                    description: 'Protocol type for the gateway. Valid values: MCP.'
                    type: string
                  roleArn:
                    description: ARN of the IAM role that the gateway assumes to access
                      AWS services.
                    type: string
                  roleArnRef:
                    description: Reference to a Role in iam to populate roleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleArnSelector:
                    description: Selector for a Role in iam to populate roleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.authorizerType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.authorizerType)
                || (has(self.initProvider) && has(self.initProvider.authorizerType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.protocolType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocolType)
                || (has(self.initProvider) && has(self.initProvider.protocolType))'
          status:
            description: GatewayStatus defines the observed state of Gateway.
            properties:
              atProvider:
                properties:
                  authorizerConfiguration:
                    description: Configuration for request authorization. Required
                      when authorizer_type is set to CUSTOM_JWT. See authorizer_configuration
                      below.
                    properties:
                      customJwtAuthorizer:
                        description: JWT-based authorization configuration block.
                          See custom_jwt_authorizer below.
                        properties:
                          allowedAudience:
                            description: Set of allowed audience values for JWT token
                              validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          allowedClients:
                            description: Set of allowed client IDs for JWT token validation.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          discoveryUrl:
                            description: URL used to fetch OpenID Connect configuration
                              or authorization server metadata. Must end with .well-known/openid-configuration.
                            type: string
                        type: object
                    type: object
                  authorizerType:
                    description: 'Type of authorizer to use. Valid values: CUSTOM_JWT,
                      AWS_IAM. When set to CUSTOM_JWT, authorizer_configuration block
                      is required.'
                    type: string
                  description:
                    description: Description of the gateway.
                    type: string
                  exceptionLevel:
                    description: 'Exception level for the gateway. Valid values: INFO,
                      WARN, ERROR.'
                    type: string
                  gatewayArn:
                    description: ARN of the Gateway.
                    type: string
                  gatewayId:
                    description: Unique identifier of the Gateway.
                    type: string
                  gatewayUrl:
                    description: URL endpoint for the gateway.
                    type: string
                  id:
                    type: string
                  interceptorConfiguration:
                    description: List of interceptor configurations for the gateway.
                      Minimum of 1, maximum of 2. See interceptor_configuration below.
                    items:
                      properties:
                        inputConfiguration:
                          description: Input configuration for the interceptor. See
                            input_configuration below.
                          properties:
                            passRequestHeaders:
                              description: Whether to pass request headers to the
                                interceptor.
                              type: boolean
                          type: object
                        interceptionPoints:
                          description: 'Set of interception points. Valid values:
                            REQUEST, RESPONSE.'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        interceptor:
                          description: Interceptor infrastructure configuration. See
                            interceptor below.
                          properties:
                            lambda:
                              description: Lambda function configuration for the interceptor.
                                See lambda below.
                              properties:
                                arn:
                                  description: ARN of the Lambda function to invoke
                                    for the interceptor.
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  kmsKeyArn:
                    description: ARN of the KMS key used to encrypt the gateway data.
                    type: string
                  name:
                    description: Name of the gateway.
                    type: string
                  protocolConfiguration:
                    description: Protocol-specific configuration for the gateway.
                      See protocol_configuration below.
                    properties:
                      mcp:
                        description: Model Context Protocol (MCP) configuration block.
                          See mcp below.
                        properties:
                          instructions:
                            description: Instructions for the MCP protocol configuration.
                            type: string
                          searchType:
                            description: 'Search type for MCP. Valid values: SEMANTIC.'
                            type: string
                          supportedVersions:
                            description: Set of supported MCP protocol versions.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  protocolType:
                    description: 'Protocol type for the gateway. Valid values: MCP.'
                    type: string
                  region:
                    description: |-
                      Region where this resource will be managed. Defaults to the Region set in the provider configuration.
                      Region is the region you'd like your resource to be created in.
                    type: string
                  roleArn:
                    description: ARN of the IAM role that the gateway assumes to access
                      AWS services.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: A map of tags assigned to the resource, including
                      those inherited from the provider default_tags configuration
                      block.
                    type: object
                    x-kubernetes-map-type: granular
                  workloadIdentityDetails:
                    description: Workload identity details for the gateway. See workload_identity_details
                      below.
                    items:
                      properties:
                        workloadIdentityArn:
                          description: ARN of the workload identity.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
