apiVersion: kafka.aws.upbound.io/v1alpha1
kind: Configuration
metadata:
  name: example
spec:
  forProvider:
    kafkaVersions:
    - 2.1.0
    name: example
    serverProperties: |
      auto.create.topics.enable = true
      delete.topic.enable = true
  providerConfigRef:
    name: example
