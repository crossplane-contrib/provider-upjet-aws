apiVersion: apigateway.aws.upbound.io/v1alpha1
kind: VPCLink
metadata:
  name: example
spec:
  forProvider:
    description: example description
    name: example
    targetArns:
    - ${aws_lb.example.arn}
  providerConfigRef:
    name: example
