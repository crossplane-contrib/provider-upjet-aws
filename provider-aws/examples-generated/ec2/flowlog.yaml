apiVersion: ec2.aws.upbound.io/v1alpha1
kind: FlowLog
metadata:
  name: example
spec:
  forProvider:
    iamRoleArnRef:
      name: example
    logDestination: ${aws_cloudwatch_log_group.example.arn}
    trafficType: ALL
    vpcIdRef:
      name: example
  providerConfigRef:
    name: example
