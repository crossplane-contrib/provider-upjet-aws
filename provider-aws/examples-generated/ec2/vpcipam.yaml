apiVersion: ec2.aws.upbound.io/v1alpha1
kind: VPCIpam
metadata:
  name: example
spec:
  forProvider:
    description: My IPAM
    operatingRegions:
    - regionName: ${data.aws_region.current.name}
    tags:
      Test: Main
  providerConfigRef:
    name: example
