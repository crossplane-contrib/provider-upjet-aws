apiVersion: ec2.aws.upbound.io/v1alpha1
kind: VPCIpamScope
metadata:
  name: example
spec:
  forProvider:
    description: Another Scope
    ipamId: ${aws_vpc_ipam.example.id}
  providerConfigRef:
    name: example
