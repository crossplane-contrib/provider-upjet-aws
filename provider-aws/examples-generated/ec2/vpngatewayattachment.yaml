apiVersion: ec2.aws.upbound.io/v1alpha1
kind: VPNGatewayAttachment
metadata:
  name: example
spec:
  forProvider:
    vpcIdRef:
      name: example
    vpnGatewayId: ${aws_vpn_gateway.vpn.id}
  providerConfigRef:
    name: example
