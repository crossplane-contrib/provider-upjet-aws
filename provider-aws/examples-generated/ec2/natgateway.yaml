apiVersion: ec2.aws.upbound.io/v1beta1
kind: NATGateway
metadata:
  name: example
spec:
  forProvider:
    allocationId: ${aws_eip.example.id}
    region: us-west-1
    subnetIdRef:
      name: example
    tags:
      Name: gw NAT
