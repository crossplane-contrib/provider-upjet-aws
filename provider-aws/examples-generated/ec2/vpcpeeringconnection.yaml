apiVersion: ec2.aws.upbound.io/v1beta1
kind: VPCPeeringConnection
metadata:
  name: example
spec:
  forProvider:
    peerOwnerId: ${var.peer_owner_id}
    peerVpcidRef:
      name: example
    region: us-west-1
    vpcIdRef:
      name: example
