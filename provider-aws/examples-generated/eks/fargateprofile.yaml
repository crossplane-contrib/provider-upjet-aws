apiVersion: eks.aws.upbound.io/v1alpha2
kind: FargateProfile
metadata:
  name: example
spec:
  forProvider:
    clusterNameRef:
      name: example
    podExecutionRoleArnRef:
      name: example
    selector:
    - namespace: example
    subnetIdRefs:
      name: example
  providerConfigRef:
    name: example
