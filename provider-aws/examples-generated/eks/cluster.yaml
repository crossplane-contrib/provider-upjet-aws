apiVersion: eks.aws.upbound.io/v1alpha2
kind: Cluster
metadata:
  name: example
spec:
  forProvider:
    roleArnRef:
      name: example
    vpcConfig:
    - subnetIdRefs:
      - name: example
  providerConfigRef:
    name: example
