apiVersion: events.aws.upbound.io/v1alpha1
kind: Archive
metadata:
  name: example
spec:
  forProvider:
    eventSourceArn: ${aws_cloudwatch_event_bus.order.arn}
    name: order-archive
  providerConfigRef:
    name: example
