apiVersion: rds.aws.upbound.io/v1beta1
kind: ClusterActivityStream
metadata:
  name: example
spec:
  forProvider:
    kmsKeyIdRef:
      name: example
    mode: async
    region: us-west-1
    resourceArn: ${aws_rds_cluster.default.arn}
