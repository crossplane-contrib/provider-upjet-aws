apiVersion: worklink.aws.upbound.io/v1alpha1
kind: WebsiteCertificateAuthorityAssociation
metadata:
  name: example
spec:
  forProvider:
    certificate: ${file("certificate.pem")}
    fleetArn: ${aws_worklink_fleet.test.arn}
  providerConfigRef:
    name: example
