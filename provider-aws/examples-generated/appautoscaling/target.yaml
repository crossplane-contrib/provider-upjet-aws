apiVersion: appautoscaling.aws.upbound.io/v1alpha1
kind: Target
metadata:
  name: example
spec:
  forProvider:
    maxCapacity: 100
    minCapacity: 5
    resourceId: GameScores
    scalableDimension: dynamodb:table:ReadCapacityUnits
    serviceNamespace: dynamodb
  providerConfigRef:
    name: example
