apiVersion: internet.aws.upbound.io/v1alpha1
kind: GatewayAttachment
metadata:
  name: example
spec:
  forProvider:
    internetGatewayId: ${aws_internet_gateway.example.id}
    vpcIdRef:
      name: example
  providerConfigRef:
    name: example
