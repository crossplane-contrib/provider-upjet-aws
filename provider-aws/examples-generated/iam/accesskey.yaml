apiVersion: iam.aws.upbound.io/v1alpha2
kind: AccessKey
metadata:
  name: example
spec:
  forProvider:
    pgpKey: keybase:some_person_that_exists
    userRef:
      name: example
  providerConfigRef:
    name: example
