apiVersion: iam.aws.upbound.io/v1alpha2
kind: GroupPolicyAttachment
metadata:
  name: example
spec:
  forProvider:
    groupRef:
      name: example
    policyArnRef:
      name: example
  providerConfigRef:
    name: example
