apiVersion: cloudfront.aws.upbound.io/v1beta1
kind: FieldLevelEncryptionProfile
metadata:
  name: example
spec:
  forProvider:
    comment: test comment
    encryptionEntities:
    - items:
      - fieldPatterns:
        - items:
          - DateOfBirth
        providerId: test provider
        publicKeyId: ${aws_cloudfront_public_key.example.id}
    name: test profile
