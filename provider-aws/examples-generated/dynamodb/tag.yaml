apiVersion: dynamodb.aws.upbound.io/v1beta1
kind: Tag
metadata:
  name: example
spec:
  forProvider:
    key: testkey
    provider: aws.replica
    region: us-west-1
    resourceArnRef:
      name: example
    value: testvalue
