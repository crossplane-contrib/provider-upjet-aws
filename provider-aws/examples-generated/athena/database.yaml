apiVersion: athena.aws.upbound.io/v1alpha1
kind: Database
metadata:
  name: example
spec:
  forProvider:
    bucket: ${aws_s3_bucket.example.bucket}
    name: database_name
  providerConfigRef:
    name: example
