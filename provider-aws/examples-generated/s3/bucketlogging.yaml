apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketLogging
metadata:
  name: example
spec:
  forProvider:
    bucket: ${aws_s3_bucket.example.id}
    region: us-west-1
    targetBucket: ${aws_s3_bucket.log_bucket.id}
    targetPrefix: log/
