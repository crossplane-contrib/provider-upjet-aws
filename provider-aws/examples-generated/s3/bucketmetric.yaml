apiVersion: s3.aws.upbound.io/v1alpha1
kind: BucketMetric
metadata:
  name: example
spec:
  forProvider:
    bucket: ${aws_s3_bucket.example.bucket}
    name: EntireBucket
  providerConfigRef:
    name: example
