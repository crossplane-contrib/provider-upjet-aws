apiVersion: connect.aws.upbound.io/v1alpha1
kind: LambdaFunctionAssociation
metadata:
  name: example
spec:
  forProvider:
    functionArn: ${aws_lambda_function.example.arn}
    instanceId: ${aws_connect_instance.example.id}
  providerConfigRef:
    name: example
