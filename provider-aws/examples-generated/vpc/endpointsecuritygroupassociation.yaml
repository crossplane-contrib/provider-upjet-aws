apiVersion: vpc.aws.upbound.io/v1alpha1
kind: EndpointSecurityGroupAssociation
metadata:
  name: example
spec:
  forProvider:
    securityGroupIdRef:
      name: example
    vpcEndpointId: ${aws_vpc_endpoint.ec2.id}
  providerConfigRef:
    name: example
