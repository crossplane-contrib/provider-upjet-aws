apiVersion: vpc.aws.upbound.io/v1alpha1
kind: IpamPreviewNextCidr
metadata:
  name: example
spec:
  forProvider:
    disallowedCidrs:
    - 172.2.0.0/32
    ipamPoolId: ${aws_vpc_ipam_pool.example.id}
    netmaskLength: 28
  providerConfigRef:
    name: example
