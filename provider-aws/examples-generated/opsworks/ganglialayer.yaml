apiVersion: opsworks.aws.upbound.io/v1alpha1
kind: GangliaLayer
metadata:
  name: example
spec:
  forProvider:
    password: foobarbaz
    stackId: ${aws_opsworks_stack.main.id}
  providerConfigRef:
    name: example
