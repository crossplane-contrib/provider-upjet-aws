apiVersion: globalaccelerator.aws.upbound.io/v1beta1
kind: Listener
metadata:
  name: example
spec:
  forProvider:
    acceleratorArnRef:
      name: example
    clientAffinity: SOURCE_IP
    portRange:
    - fromPort: 80
      toPort: 80
    protocol: TCP
    region: us-west-1
