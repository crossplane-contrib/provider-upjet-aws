apiVersion: globalaccelerator.aws.upbound.io/v1alpha2
kind: Listener
metadata:
  name: example
spec:
  forProvider:
    acceleratorArnRef:
      name: example
    clientAffinity: SOURCE_IP
    portRange:
    - fromPort: 80
      toPort: 80
    protocol: TCP
  providerConfigRef:
    name: example
