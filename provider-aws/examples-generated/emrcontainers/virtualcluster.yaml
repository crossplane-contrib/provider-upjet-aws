apiVersion: emrcontainers.aws.upbound.io/v1alpha1
kind: VirtualCluster
metadata:
  name: example
spec:
  forProvider:
    containerProvider:
    - id: ${aws_eks_cluster.example.name}
      info:
      - eksInfo:
        - namespace: default
      type: EKS
    name: example
  providerConfigRef:
    name: example
