apiVersion: efs.aws.upbound.io/v1alpha1
kind: BackupPolicy
metadata:
  name: example
spec:
  forProvider:
    backupPolicy:
    - status: ENABLED
    fileSystemId: ${aws_efs_file_system.fs.id}
  providerConfigRef:
    name: example
