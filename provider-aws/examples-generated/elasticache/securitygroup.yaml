apiVersion: elasticache.aws.upbound.io/v1alpha1
kind: SecurityGroup
metadata:
  name: example
spec:
  forProvider:
    name: elasticache-security-group
    securityGroupNames:
    - ${aws_security_group.bar.name}
  providerConfigRef:
    name: example
