apiVersion: elasticache.aws.upbound.io/v1alpha1
kind: UserGroupAssociation
metadata:
  name: example
spec:
  forProvider:
    userGroupId: ${aws_elasticache_user_group.example.user_group_id}
    userId: ${aws_elasticache_user.example.user_id}
  providerConfigRef:
    name: example
