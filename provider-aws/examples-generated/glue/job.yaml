apiVersion: glue.aws.upbound.io/v1beta1
kind: Job
metadata:
  name: example
spec:
  forProvider:
    command:
    - scriptLocation: s3://${aws_s3_bucket.example.bucket}/example.py
    roleArnRef:
      name: example
