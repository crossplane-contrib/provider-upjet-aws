apiVersion: kms.aws.upbound.io/v1beta1
kind: Ciphertext
metadata:
  name: example
spec:
  forProvider:
    keyIdRef:
      name: example
    plaintextSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
    region: us-west-1
