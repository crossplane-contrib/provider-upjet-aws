apiVersion: kms.aws.upbound.io/v1alpha1
kind: Alias
metadata:
  name: example
spec:
  forProvider:
    name: alias/my-key-alias
    targetKeyId: ${aws_kms_key.a.key_id}
  providerConfigRef:
    name: example
