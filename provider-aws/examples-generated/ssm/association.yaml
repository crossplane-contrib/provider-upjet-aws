apiVersion: ssm.aws.upbound.io/v1alpha1
kind: Association
metadata:
  name: example
spec:
  forProvider:
    name: test_document
    targets:
    - key: InstanceIds
      values:
      - ${aws_instance.example.id}
  providerConfigRef:
    name: example
