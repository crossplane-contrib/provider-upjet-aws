apiVersion: route53resolver.aws.upbound.io/v1alpha1
kind: QueryLogConfigAssociation
metadata:
  name: example
spec:
  forProvider:
    resolverQueryLogConfigId: ${aws_route53_resolver_query_log_config.example.id}
    resourceId: ${aws_vpc.example.id}
  providerConfigRef:
    name: example
