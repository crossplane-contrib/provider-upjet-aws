apiVersion: route53resolver.aws.upbound.io/v1alpha1
kind: QueryLogConfig
metadata:
  name: example
spec:
  forProvider:
    destinationArn: ${aws_s3_bucket.example.arn}
    name: example
    tags:
      Environment: Prod
  providerConfigRef:
    name: example
