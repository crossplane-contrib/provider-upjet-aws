apiVersion: ds.aws.upbound.io/v1alpha1
kind: ConditionalForwarder
metadata:
  name: example
spec:
  forProvider:
    directoryId: ${aws_directory_service_directory.ad.id}
    dnsIps:
    - 8.8.8.8
    - 8.8.4.4
    remoteDomainName: example.com
  providerConfigRef:
    name: example
