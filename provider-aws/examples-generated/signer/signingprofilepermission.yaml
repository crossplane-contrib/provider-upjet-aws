apiVersion: signer.aws.upbound.io/v1alpha1
kind: SigningProfilePermission
metadata:
  name: example
spec:
  forProvider:
    action: signer:StartSigningJob
    principal: ${var.aws_account}
    profileName: ${aws_signer_signing_profile.prod_sp.name}
  providerConfigRef:
    name: example
