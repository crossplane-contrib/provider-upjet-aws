apiVersion: apigatewayv2.aws.upbound.io/v1alpha1
kind: APIMapping
metadata:
  name: example
spec:
  forProvider:
    apiId: ${aws_apigatewayv2_api.example.id}
    domainName: ${aws_apigatewayv2_domain_name.example.id}
    stage: ${aws_apigatewayv2_stage.example.id}
  providerConfigRef:
    name: example
