apiVersion: apigatewayv2.aws.upbound.io/v1alpha1
kind: Route
metadata:
  name: example
spec:
  forProvider:
    apiId: ${aws_apigatewayv2_api.example.id}
    routeKey: $default
  providerConfigRef:
    name: example
