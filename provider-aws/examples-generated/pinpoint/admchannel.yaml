apiVersion: pinpoint.aws.upbound.io/v1alpha1
kind: ADMChannel
metadata:
  name: example
spec:
  forProvider:
    applicationId: ${aws_pinpoint_app.app.application_id}
    clientIdSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
    clientSecretSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
    enabled: true
  providerConfigRef:
    name: example
