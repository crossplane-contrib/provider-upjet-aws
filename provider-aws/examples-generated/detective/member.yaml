apiVersion: detective.aws.upbound.io/v1alpha1
kind: Member
metadata:
  name: example
spec:
  forProvider:
    accountId: AWS ACCOUNT ID
    disableEmailNotification: true
    emailAddress: EMAIL
    graphArn: ${aws_detective_graph.example.id}
    message: Message of the invitation
  providerConfigRef:
    name: example
