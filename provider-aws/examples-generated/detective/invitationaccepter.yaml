apiVersion: detective.aws.upbound.io/v1alpha1
kind: InvitationAccepter
metadata:
  name: example
spec:
  forProvider:
    graphArn: ${aws_detective_graph.primary.graph_arn}
    provider: awsalternate
  providerConfigRef:
    name: example
