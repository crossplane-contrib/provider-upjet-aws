apiVersion: mediapackage.aws.upbound.io/v1alpha1
kind: Channel
metadata:
  name: example
spec:
  forProvider:
    channelId: kitten-channel
    description: A channel dedicated to amusing videos of kittens.
  providerConfigRef:
    name: example
