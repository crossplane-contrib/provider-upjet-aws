apiVersion: cloudtrail.aws.upbound.io/v1alpha1
kind: Trail
metadata:
  name: example
spec:
  forProvider:
    includeGlobalServiceEvents: false
    name: tf-trail-foobar
    s3BucketName: ${aws_s3_bucket.foo.id}
    s3KeyPrefix: prefix
  providerConfigRef:
    name: example
