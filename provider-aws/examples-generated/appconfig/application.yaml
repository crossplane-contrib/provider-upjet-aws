apiVersion: appconfig.aws.upbound.io/v1alpha1
kind: Application
metadata:
  name: example
spec:
  forProvider:
    description: Example AppConfig Application
    name: example-application-tf
    tags:
      Type: AppConfig Application
  providerConfigRef:
    name: example
