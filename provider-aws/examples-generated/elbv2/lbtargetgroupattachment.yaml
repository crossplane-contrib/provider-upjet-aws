apiVersion: elbv2.aws.upbound.io/v1beta1
kind: LBTargetGroupAttachment
metadata:
  name: example
spec:
  forProvider:
    port: 80
    region: us-west-1
    targetGroupArnRef:
      name: example
    targetId: ${aws_instance.test.id}
