apiVersion: cognito.aws.upbound.io/v1alpha1
kind: User
metadata:
  name: example
spec:
  forProvider:
    userPoolId: ${aws_cognito_user_pool.example.id}
    username: example
  providerConfigRef:
    name: example
