---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: buckets.s3.aws.upbound.io
spec:
  group: s3.aws.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: Bucket
    listKind: BucketList
    plural: buckets
    singular: bucket
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Bucket is the Schema for the Buckets API. Provides a S3 bucket
          resource.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BucketSpec defines the desired state of Bucket
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  forceDestroy:
                    description: A boolean that indicates all objects  should be deleted
                      from the bucket so that the bucket can be destroyed without
                      error. These objects are not recoverable.
                    type: boolean
                  hostedZoneId:
                    description: The Route 53 Hosted Zone ID for this bucket's region.
                    type: string
                  objectLockEnabled:
                    type: boolean
                  region:
                    description: The AWS region this bucket resides in. Region is
                      the region you'd like your resource to be created in.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                required:
                - region
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: BucketStatus defines the observed state of Bucket.
            properties:
              atProvider:
                properties:
                  accelerationStatus:
                    description: Sets the accelerate configuration of an existing
                      bucket. Can be Enabled or Suspended. Cannot be used in cn-north-1
                      or us-gov-west-1. Terraform will only perform drift detection
                      if a configuration value is provided. Use the resource aws_s3_bucket_accelerate_configuration
                      instead.
                    type: string
                  acl:
                    description: The canned ACL to apply. Valid values are private,
                      public-read, public-read-write, aws-exec-read, authenticated-read,
                      and log-delivery-write. Defaults to private.  Conflicts with
                      grant. Terraform will only perform drift detection if a configuration
                      value is provided. Use the resource aws_s3_bucket_acl instead.
                    type: string
                  arn:
                    description: The ARN of the bucket. Will be of format arn:aws:s3:::bucketname.
                    type: string
                  bucketDomainName:
                    description: The bucket domain name. Will be of format bucketname.s3.amazonaws.com.
                    type: string
                  bucketRegionalDomainName:
                    description: 'The bucket region-specific domain name. The bucket
                      domain name including the region name, please refer here for
                      format. Note: The AWS CloudFront allows specifying S3 region-specific
                      endpoint when creating S3 origin, it will prevent redirect issues
                      from CloudFront to S3 Origin URL.'
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing. See CORS
                      rule below for details. Terraform will only perform drift detection
                      if a configuration value is provided. Use the resource aws_s3_bucket_cors_configuration
                      instead.
                    items:
                      properties:
                        allowedHeaders:
                          description: List of headers allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: One or more HTTP methods that you allow the
                            origin to execute. Can be GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: One or more origins you want customers to be
                            able to access the bucket from.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: One or more headers in the response that you
                            want customers to be able to access from their applications
                            .
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  grant:
                    description: An ACL policy grant. See Grant below for details.
                      Conflicts with acl. Terraform will only perform drift detection
                      if a configuration value is provided. Use the resource aws_s3_bucket_acl
                      instead.
                    items:
                      properties:
                        id:
                          type: string
                        permissions:
                          description: List of permissions to apply for grantee. Valid
                            values are READ, WRITE, READ_ACP, WRITE_ACP, FULL_CONTROL.
                          items:
                            type: string
                          type: array
                        type:
                          description: Type of grantee to apply for. Valid values
                            are CanonicalUser and Group. AmazonCustomerByEmail is
                            not supported.
                          type: string
                        uri:
                          description: Uri address to grant for. Used only when type
                            is Group.
                          type: string
                      type: object
                    type: array
                  id:
                    type: string
                  lifecycleRule:
                    description: A configuration of object lifecycle management. See
                      Lifecycle Rule below for details. Terraform will only perform
                      drift detection if a configuration value is provided. Use the
                      resource aws_s3_bucket_lifecycle_configuration instead.
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                          type: number
                        enabled:
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire.
                            See Expiration below for details.
                          items:
                            properties:
                              date:
                                type: string
                              days:
                                type: number
                              expiredObjectDeleteMarker:
                                description: On a versioned bucket , you can add this
                                  element in the lifecycle configuration to direct
                                  Amazon S3 to delete expired object delete markers.
                                  This cannot be specified with Days or Date in a
                                  Lifecycle Expiration Policy.
                                type: boolean
                            type: object
                          type: array
                        id:
                          type: string
                        noncurrentVersionExpiration:
                          description: Specifies when noncurrent object versions expire.
                            See Noncurrent Version Expiration below for details.
                          items:
                            properties:
                              days:
                                type: number
                            type: object
                          type: array
                        noncurrentVersionTransition:
                          description: Specifies when noncurrent object versions transitions.
                            See Noncurrent Version Transition below for details.
                          items:
                            properties:
                              days:
                                type: number
                              storageClass:
                                type: string
                            type: object
                          type: array
                        prefix:
                          type: string
                        tags:
                          additionalProperties:
                            type: string
                          type: object
                        transition:
                          description: Specifies a period in the object's transitions.
                            See Transition below for details.
                          items:
                            properties:
                              date:
                                type: string
                              days:
                                type: number
                              storageClass:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  logging:
                    description: A configuration of S3 bucket logging parameters.
                      See Logging below for details. Terraform will only perform drift
                      detection if a configuration value is provided. Use the resource
                      aws_s3_bucket_logging instead.
                    items:
                      properties:
                        targetBucket:
                          description: The name of the bucket that will receive the
                            log objects.
                          type: string
                        targetPrefix:
                          description: To specify a key prefix for log objects.
                          type: string
                      type: object
                    type: array
                  objectLockConfiguration:
                    description: A configuration of S3 object locking. See Object
                      Lock Configuration below for details. Terraform wil only perform
                      drift detection if a configuration value is provided. Use the
                      object_lock_enabled parameter and the resource aws_s3_bucket_object_lock_configuration
                      instead.
                    items:
                      properties:
                        objectLockEnabled:
                          type: string
                        rule:
                          items:
                            properties:
                              defaultRetention:
                                description: The default retention period that you
                                  want to apply to new objects placed in this bucket
                                  .
                                items:
                                  properties:
                                    days:
                                      type: number
                                    mode:
                                      description: The default Object Lock retention
                                        mode you want to apply to new objects placed
                                        in this bucket. Valid values are GOVERNANCE
                                        and COMPLIANCE.
                                      type: string
                                    years:
                                      description: The number of years that you want
                                        to specify for the default retention period.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  policy:
                    description: A valid bucket policy JSON document. Note that if
                      the policy document is not specific enough , Terraform may view
                      the policy as constantly changing in a terraform plan. In this
                      case, please make sure you use the verbose/specific version
                      of the policy. For more information about building AWS IAM policy
                      documents with Terraform, see the AWS IAM Policy Document Guide.
                      Terraform will only perform drift detection if a configuration
                      value is provided. Use the resource aws_s3_bucket_policy instead.
                    type: string
                  replicationConfiguration:
                    description: A configuration of replication configuration. See
                      Replication Configuration below for details. Terraform will
                      only perform drift detection if a configuration value is provided.
                      Use the resource aws_s3_bucket_replication_configuration instead.
                    items:
                      properties:
                        role:
                          description: The ARN of the IAM role for Amazon S3 to assume
                            when replicating the objects.
                          type: string
                        rules:
                          description: Specifies the rules managing the replication
                            .
                          items:
                            properties:
                              deleteMarkerReplicationStatus:
                                description: Whether delete markers are replicated.
                                  The only valid value is Enabled. To disable, omit
                                  this argument. This argument is only valid with
                                  V2 replication configurations .
                                type: string
                              destination:
                                description: Specifies the destination for the rule
                                  .
                                items:
                                  properties:
                                    accessControlTranslation:
                                      description: Specifies the overrides to use
                                        for object owners on replication. Must be
                                        used in conjunction with account_id owner
                                        override configuration.
                                      items:
                                        properties:
                                          owner:
                                            type: string
                                        type: object
                                      type: array
                                    accountId:
                                      description: The Account ID to use for overriding
                                        the object owner on replication. Must be used
                                        in conjunction with access_control_translation
                                        override configuration.
                                      type: string
                                    bucket:
                                      type: string
                                    metrics:
                                      description: Enables replication metrics  .
                                      items:
                                        properties:
                                          minutes:
                                            type: number
                                          status:
                                            type: string
                                        type: object
                                      type: array
                                    replicaKmsKeyId:
                                      description: Destination KMS encryption key
                                        ARN for SSE-KMS replication. Must be used
                                        in conjunction with sse_kms_encrypted_objects
                                        source selection criteria.
                                      type: string
                                    replicationTime:
                                      description: Enables S3 Replication Time Control  .
                                      items:
                                        properties:
                                          minutes:
                                            type: number
                                          status:
                                            type: string
                                        type: object
                                      type: array
                                    storageClass:
                                      type: string
                                  type: object
                                type: array
                              filter:
                                description: Filter that identifies subset of objects
                                  to which the replication rule applies .
                                items:
                                  properties:
                                    prefix:
                                      type: string
                                    tags:
                                      additionalProperties:
                                        type: string
                                      type: object
                                  type: object
                                type: array
                              id:
                                type: string
                              prefix:
                                type: string
                              priority:
                                description: The priority associated with the rule.
                                  Priority should only be set if filter is configured.
                                  If not provided, defaults to 0. Priority must be
                                  unique between multiple rules.
                                type: number
                              sourceSelectionCriteria:
                                description: Specifies special object selection criteria
                                  .
                                items:
                                  properties:
                                    sseKmsEncryptedObjects:
                                      description: Match SSE-KMS encrypted objects
                                        . If specified, replica_kms_key_id in destination
                                        must be specified as well.
                                      items:
                                        properties:
                                          enabled:
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              status:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  requestPayer:
                    description: Specifies who should bear the cost of Amazon S3 data
                      transfer. Can be either BucketOwner or Requester. By default,
                      the owner of the S3 bucket would incur the costs of any data
                      transfer. See Requester Pays Buckets developer guide for more
                      information. Terraform will only perform drift detection if
                      a configuration value is provided. Use the resource aws_s3_bucket_request_payment_configuration
                      instead.
                    type: string
                  serverSideEncryptionConfiguration:
                    description: A configuration of server-side encryption configuration.
                      See Server Side Encryption Configuration below for details.
                      Terraform will only perform drift detection if a configuration
                      value is provided. Use the resource aws_s3_bucket_server_side_encryption_configuration
                      instead.
                    items:
                      properties:
                        rule:
                          items:
                            properties:
                              applyServerSideEncryptionByDefault:
                                description: A single object for setting server-side
                                  encryption by default.
                                items:
                                  properties:
                                    kmsMasterKeyId:
                                      description: The AWS KMS master key ID used
                                        for the SSE-KMS encryption. This can only
                                        be used when you set the value of sse_algorithm
                                        as aws:kms. The default aws/s3 AWS KMS master
                                        key is used if this element is absent while
                                        the sse_algorithm is aws:kms.
                                      type: string
                                    sseAlgorithm:
                                      description: The server-side encryption algorithm
                                        to use. Valid values are AES256 and aws:kms
                                      type: string
                                  type: object
                                type: array
                              bucketKeyEnabled:
                                description: Whether or not to use Amazon S3 Bucket
                                  Keys for SSE-KMS.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: A map of tags assigned to the resource, including
                      those inherited from the provider default_tags configuration
                      block.
                    type: object
                  versioning:
                    description: A configuration of the S3 bucket versioning state.
                      See Versioning below for details. Terraform will only perform
                      drift detection if a configuration value is provided. Use the
                      resource aws_s3_bucket_versioning instead.
                    items:
                      properties:
                        enabled:
                          type: boolean
                        mfaDelete:
                          description: Enable MFA delete for either Change the versioning
                            state of your bucket or Permanently delete an object version.
                            Default is false. This cannot be used to toggle this setting
                            but is available to allow managed buckets to reflect the
                            state in AWS
                          type: boolean
                      type: object
                    type: array
                  website:
                    description: A configuration of the S3 bucket website. See Website
                      below for details. Terraform will only perform drift detection
                      if a configuration value is provided. Use the resource aws_s3_bucket_website_configuration
                      instead.
                    items:
                      properties:
                        errorDocument:
                          description: An absolute path to the document to return
                            in case of a 4XX error.
                          type: string
                        indexDocument:
                          description: Amazon S3 returns this index document when
                            requests are made to the root domain or any of the subfolders.
                          type: string
                        redirectAllRequestsTo:
                          description: A hostname to redirect all website requests
                            for this bucket to. Hostname can optionally be prefixed
                            with a protocol  to use when redirecting requests. The
                            default is the protocol that is used in the original request.
                          type: string
                        routingRules:
                          description: A json array containing routing rules describing
                            redirect behavior and when redirects are applied.
                          type: string
                      type: object
                    type: array
                  websiteDomain:
                    description: The domain of the website endpoint, if the bucket
                      is configured with a website. If not, this will be an empty
                      string. This is used to create Route 53 alias records.
                    type: string
                  websiteEndpoint:
                    description: The website endpoint, if the bucket is configured
                      with a website. If not, this will be an empty string.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
