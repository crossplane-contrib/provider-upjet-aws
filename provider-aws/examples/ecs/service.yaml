apiVersion: ecs.aws.upbound.io/v1beta1
kind: Service
metadata:
  name: sample-service
spec:
  forProvider:
    taskDefinition: service
    cluster: example
    region: us-west-1
    launchType: EC2
    propagateTags: TASK_DEFINITION
