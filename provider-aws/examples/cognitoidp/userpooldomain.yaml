apiVersion: cognitoidp.aws.upbound.io/v1beta1
kind: UserPoolDomain
metadata:
  name: example
spec:
  forProvider:
    domain: example-domain
    region: us-west-1
    userPoolIdRef:
      name: example
