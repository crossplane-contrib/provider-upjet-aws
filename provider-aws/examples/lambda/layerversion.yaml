apiVersion: lambda.aws.upbound.io/v1beta1
kind: LayerVersion
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    compatibleRuntimes:
    - nodejs12.x
    filename: example.zip
    layerName: lambda_layer_name
