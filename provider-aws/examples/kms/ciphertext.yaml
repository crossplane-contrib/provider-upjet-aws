apiVersion: kms.aws.upbound.io/v1beta1
kind: Ciphertext
metadata:
  name: example
spec:
  forProvider:
    region: us-east-1
    keyIdRef:
      name: sample-key
    plaintextSecretRef:
      key: example-key
      name: example-secret
      namespace: crossplane-system
