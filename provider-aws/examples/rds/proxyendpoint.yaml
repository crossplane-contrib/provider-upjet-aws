apiVersion: rds.aws.upbound.io/v1beta1
kind: ProxyEndpoint
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    dbProxyEndpointName: example
    dbProxyName: example
    targetRole: READ_ONLY
    vpcSubnetIds:
      - example1
      - example2
  providerConfigRef:
    name: default
