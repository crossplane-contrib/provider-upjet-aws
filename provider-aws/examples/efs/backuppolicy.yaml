apiVersion: efs.aws.upbound.io/v1beta1
kind: BackupPolicy
metadata:
  name: example
spec:
  forProvider:
    backupPolicy:
    - status: ENABLED
    fileSystemIdRef:
      name: example
    region: us-west-1
