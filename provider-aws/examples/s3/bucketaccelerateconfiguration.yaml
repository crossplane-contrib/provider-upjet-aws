apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketAccelerateConfiguration
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: upbound-example
    status: Enabled
  providerConfigRef:
    name: default