apiVersion: s3.aws.upbound.io/v1alpha1
kind: BucketObject
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: example
    key: new_object_key
  providerConfigRef:
    name: default

