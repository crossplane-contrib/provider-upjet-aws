apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketLogging
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: upbound-example
    targetBucket: upbound-example-2
    targetPrefix: log/
  providerConfigRef:
    name: default