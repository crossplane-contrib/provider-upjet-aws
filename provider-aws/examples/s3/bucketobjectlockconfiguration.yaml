apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketObjectLockConfiguration
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: example
    rule:
    - defaultRetention:
      - days: 1
        mode: COMPLIANCE
  providerConfigRef:
    name: default

