apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketRequestPaymentConfiguration
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: upbound-example
    payer: Requester
  providerConfigRef:
    name: default
