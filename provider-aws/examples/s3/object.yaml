apiVersion: s3.aws.upbound.io/v1beta1
kind: BucketObject
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    bucket: upbound-example
    key: object_key
    contentBase64: dGhpcyBpcyBhIHRleHQg
  providerConfigRef:
    name: default
