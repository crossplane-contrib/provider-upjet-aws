apiVersion: ec2.aws.upbound.io/v1beta1
kind: NATGateway
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    connectivityType: "private"
    subnetIdRef:
      name: example
    tags:
      Name: gw NAT
