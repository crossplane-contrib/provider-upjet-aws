apiVersion: ec2.aws.upbound.io/v1beta1
kind: PlacementGroup
metadata:
  labels:
    testing.upbound.io/example-name: web
  name: web
spec:
  forProvider:
    region: us-west-1
    strategy: cluster

---

