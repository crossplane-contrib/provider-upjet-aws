apiVersion: ec2.aws.upbound.io/v1beta1
kind: Route
metadata:
  name: example
spec:
  forProvider:
    region: us-west-1
    destinationCidrBlock: 0.0.0.0/0
    gatewayIdRef:
      name: example
    routeTableIdRef:
      name: example
  providerConfigRef:
    name: default